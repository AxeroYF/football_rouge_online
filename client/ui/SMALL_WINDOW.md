# 小型窗口

“小型窗口”是黄狗风云的档案、详情和确认类模态窗口标准，尺寸与当前球员26项数值详情窗口一致。

- 最大宽度：1180px。
- 桌面内边距：24px；中等屏幕 10px；手机全屏。
- 最大高度：容器高度减 8px，内容超出后内部滚动。
- 默认带遮罩和轻微模糊。
- Esc、关闭按钮和点击遮罩关闭。
- 作为模态窗口锁定交互并自动聚焦内容层。

使用 `bindSmallWindow` 绑定生命周期。根层使用 `small-window`，内容层使用 `small-window__dialog`；旧的26项数值窗口已作为首个兼容实现接入。

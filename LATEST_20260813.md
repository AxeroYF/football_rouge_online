# YellowDogs League S4 最新交接状态（2026-08-13）

## 18:49 最新补充

- 最新完整对话交接包：`handoff/football-ydl-s4-handoff-20260813-1849.zip`；仅用于恢复开发上下文，不是生产部署包。
- 最新合并生产热更新：`handoff/football-ydl-s4-recent-batch-hotfix-20260813-183713.tar.gz`；SHA256 `649b46dd3a47f5c57ad74e244564cb60fd439495445e5e2cbf879ecfd3e29fc2`；尚未确认服务器已部署。
- 本队数据榜单已合并联赛和杯赛；荣誉室射手榜、助攻榜、红牌榜分别显示前三名，并支持同队多名金球奖得主。
- V2.1 点球大战每两秒逐条播报；战术方案增加 revision 和当前方案校验，旧设备/卡顿请求不能覆盖较新方案。
- 休赛期友谊赛正常判定受伤、伤退和播报，但赛后不会形成正式伤停；规则以实际开赛时状态固化，避免跨赛季结算误判。
- 验证通过：荣誉室/V2.1/分片 19/19，战术并发/综合榜单/友谊赛/延迟开服 6/6，生产包解包哈希与 JavaScript 语法检查通过。
- 当前 Git 基线是 `40e22d2`（tag `S4-0813`），本批代码与测试仍未提交，禁止清理或重置工作树。

## 工作区与安全边界

- 最新完整对话交接包：`handoff/football-ydl-s4-handoff-20260813-1747.zip`。
- 继续在 `D:\Project\game_test\.worktrees\s4` 工作，保留当前未提交工作树；禁止 `reset`、`clean`、整体 `checkout` 或覆盖运行存档。
- 本文档对应的对话交接包仅用于恢复开发上下文，不是生产部署包，不得整体覆盖服务器。
- 包内排除 `data/` 运行数据、线上分片、`outputs/` 大型模拟结果、账号密钥、旧交接目录、历史备份和临时素材。

## 8 月 13 日联赛重启与 502 修复

- 每日赛季重启 OOM 根因位于归档流程：分片模式曾深拷贝完整赛季、比赛和球员统计，并在保存时反复读取/比较历史归档，容易在大存档上造成内存峰值。
- `versus/league-service.js` 在分片模式下不再额外深拷贝归档；后台归档摘要兼容 `matchCount`。
- `versus/league-shard-store.js` 重载历史归档时只读取 manifest 摘要，不把完整历史比赛重新装回内存；已有归档文件不再重复写入。
- 延迟开服或每日重启完成后，联赛首轮统一排到当前时间之后的下一个合法时隙；杯赛按后续时隙启动，不会在服务刚启动时立即开赛。
- 后台将 409 存档冲突与 502/503/504 服务不可用分开显示，网关错误不再错误引导运营人员“开启全新赛季”。
- 对应生产热修包为 `football-ydl-s4-daily-reset-oom-schedule-hotfix-20260813-102452.tar.gz`，但对话交接包仍以当前工作树源码为准。

## 最近 UI 与战术板改动

- 浏览器刷新恢复刷新前页面，而不是跳回登录页或固定落到首页。
- 战术方案最多三套、至少保留一套；已补充删除能力并修复确认弹窗卡住问题。
- 战术板导入/导出使用 9 位阵容码、24 小时有效；仅导入三个阶段的站位、职责和战术设置，不导入其他玩家的球员身份。
- 导入、导出按钮已移动到方案控制区，战术节点按钮调整位置；三个阶段按钮缩窄并保持等宽。
- 手机端邮件详情、友谊赛邀请操作与杯赛积分榜信息布局已有优化；此前一次“合卡仓库特性显示”改动已按用户要求回退，不要误恢复。
- 伤停播报与实际伤停轮次已统一；体力红线自动换人逻辑及相关最新修改需保留。

## V2.1 引擎与模拟

- 最新平衡报告位于 `outputs/S4-V2.1-balance-postfix-full-20260812.json`；维度分片和诊断样本由用户另行保存，默认不装入交接 ZIP。
- 当前预测市场口径必须保持：赔率样本由 V1 引擎生成，实际比赛结果由 V2 引擎结算。
- 新增 `devtool/simulate-prediction-market.js`：默认 18 赛季、每季 90 场，共 1620 场；本机预计约 3 小时。
- 模拟启动即输出进度，之后每完成一轮输出百分比、轮数、已用时间和预计剩余时间。
- 模拟修复了跨赛季市场 ID 可能重复，以及每场在线性扫描全部历史记录导致越跑越慢的问题；当前轮次改用直接索引。
- 默认输出为 `outputs/prediction-market-simulation.json`，该结果文件不包含在交接包内。

## 当前未提交文件

- `admin/public/app.js`
- `versus/league-service.js`
- `versus/league-shard-store.js`
- `devtool/simulate-prediction-market.js`
- `test/admin.test.js`
- `test/league.test.js`
- `test/league-shards.test.js`
- `handoff/` 本轮更新文档

`data/ydl-player-card-studio.json*`、`history/` 和旧交接展开目录属于本地运行/历史资料，不应提交或打包。

## 验证与下一步

- 预测模拟脚本已通过 `node --check` 和实际启动进度验证；完整三小时模拟由用户自行运行。
- 每日重启、分片归档和后台错误页已有对应定向测试；接手后先运行这些定向测试，再决定是否运行全套测试。
- 不要同时启动多个预测模拟进程；如果终端没有进度，先确认旧 Node 进程已经停止并重新启动当前脚本。

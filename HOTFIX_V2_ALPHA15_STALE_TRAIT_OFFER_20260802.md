# YDL S4 V2 alpha.15 强化特性悬空状态热更新

本包完整覆盖 alpha.14，不包含联赛存档、账号、备份或服务器配置。

## 问题

- 强化到 +4 或 +7 后生成的待绑定特性属于具体球员卡。
- 该卡未绑定特性便被作为强化副卡消耗时，旧版没有取消对应的待绑定记录。
- 后续强化结果会错误回退到账号中的旧记录，导致任意等级都显示“绑定特性”，但服务端因原卡已失效而拒绝绑定。

## 修复

- 待绑定卡作为副卡消耗时，立即取消该卡对应的 pending 特性记录。
- 读取联赛视图和执行强化前，自动清理卡片已回收、所有者不符、等级门槛不符或已经满足特性数量的悬空记录。
- 强化结果界面只使用本次强化返回的特性记录，不再回退到账号中的旧记录。
- 正常的 +4 和 +7 特性解锁规则保持不变。
- 现有服务器脏数据无需手工修改；部署后玩家打开联赛页面即自动失效。
- 保留 alpha.14 的预测轻量接口、局部刷新和 alpha.13 的逐场直播检查点。

## 验证

- 待绑定 +4 卡作为副卡消耗后旧记录取消：通过。
- 后续 +0 到 +1 强化不再显示绑定特性：通过。
- 前端不再回退悬空记录：通过。
- 普通卡 +4/+7 特性门槛和轻量强化响应回归：通过。
- JavaScript 语法与 `git diff --check`：通过。

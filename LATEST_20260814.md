# YellowDogs League S4 最新交接状态（2026-08-14 17:40）

## 当前最高优先级

- 唯一工作区：`D:\Project\game_test\.worktrees\s4`；Git 基线 `40e22d2`，分支 `codex/v2.1-dynamic`，存在大量未提交成果，禁止 `reset`、`clean`、整体 `checkout` 或覆盖工作树。
- 本交接 ZIP 只用于恢复本地开发上下文，不是服务器部署包；禁止整体上传，禁止覆盖服务器 `data`、分片目录、manifest、revision、账号或密钥。
- 用户已确认预测市场 V9 于 2026-08-14 上午上线。当前最新生产候选为 `handoff/football-ydl-s4-post-v9-ballon-recovery-tactics-hotfix-20260814-191936.tar.gz`，SHA256 `a449e905fa84a6d0fa01533d610879d6e36dc13e2123c03b5e5f694450251b46`；这是 V9 之后的 4 文件最小增量包，尚未确认线上部署。

## 预测市场 V9

- 盘口仍由 V1 快速引擎生成、实赛由 V2 结算，保留两者数据差以避免预测市场成为真实胜率查询器。
- 胜平负让球压缩到 `-2..+2`，赔率整体折算约 85%，更集中削减高赔率，并加入受控赔率混淆。
- 已针对“非零盘口指向的强队”可稳定获利路径继续压低预期收益。
- 已移除射正总数玩法；保留胜平负、总进球、红黄牌，并新增半全场九种结果。半全场根据主客强弱、半场与全场联合概率定价，投注上限为 5000 金币。
- 前台不显示预计返还，赔率数字不带“倍”；后台预测监测入口放在独立区域，可查看当天玩家投注、已结算/未结算与收益。
- 并行模拟脚本：`devtool/simulate-prediction-market-parallel.js`；用户曾使用 15 核运行 15 赛季。大型 `outputs/` 结果不进入交接包。

## 金球奖 V3（本轮最新改动）

- 新积分：联赛冠军 35、联赛亚军 15、杯赛冠军 30、杯赛亚军 10；进球、助攻、评分、15 场门槛、杯赛 1.25 倍权重保持不变。
- 新赛季结算结果增加冠亚军明细和四项积分拆分，结算邮件同步说明。
- 部署后首次启动会自动建立 `pre-ballon-dor-reconciliation-v1` 备份，并对已有完整赛季归档执行一次历史复核。
- 改判时收回原球员卡金球标识，向新得主球员卡补发标识并修正荣誉室；原得主已发金币不追回。
- 新得主若该赛季没有金球奖励流水，补发 10000 金币；已有记录不重复发放。
- 所有真人玩家各收到一封统一公告，公示规则、每届旧/新得主、标识处理和金币处理；复核有版本标记，重启不会重复发币或发信。
- 若某届缺少完整归档，系统保留原结果并在公告中明确说明，不使用旧 TOP10 猜测完整排名。
- 定向验证：`node --test --test-name-pattern="Ballon d'Or" test/league.test.js`，3/3 通过；未运行全量测试。

## 战术方案覆盖问题（已诊断，尚未修复）

- 普通战术保存已有当前方案 ID + revision 双重校验，旧页面保存和旧页面切换会被拒绝。
- 阵容码导入仍缺少当前方案 ID/revision 校验，并会把方案 revision 重置为 0；多设备并发时可能覆盖错误方案。
- 离开战术页时前端未等待异步保存便清除 dirty 标记，保存失败后可能丢失本地改动。
- 重命名/赛事适配在同一激活方案下缺少版本校验；页面立即关闭也没有可靠的未保存保护。
- 这些问题只完成了代码审查，没有修复、测试或打包；接手后若用户要求，应先修复再制作生产包。

## 体力自动换人与其他关键口径

- 体力红线自动换人选择应按强化后的真实总评排序，而不是基础默认总评；相关实现和测试位于 `versus/automatic-substitution.js`、`versus/league-service.js`、`test/automatic-substitution.test.js`。
- 预测市场系统投注收益分红、赔率调整及后台监测均包含在当前脏工作树中；不要回退此前的预测 V9 改动。

## 系统单卡回收上限（2026-08-14 后续改动）

- 普通单卡和批量单卡系统回收现允许强化等级 `+0..+6`；`+7`、`+8` 继续受保护且不能通过该入口回收。
- X 级球员不可回收、最后一张所有权锚点卡、挂牌和交易锁定等既有保护规则保持不变；所有权返还清算规则未修改。
- 回收定向测试 12/12、JavaScript 语法和差异检查通过。完整 `test/s4-assets.test.js` 为 109/114；5 项失败属于此前工作树中的复盘刷新、手机战术控件、预测显示、X 球员任务和世界杯移除断言，与本次回收上限改动无关。
- 本改动尚未制作生产热更新包。

## 战术自动保存状态位置（2026-08-14 后续改动）

- 自动保存状态已从细节战术面板右下角移到替补席栏底部左侧，使用独立固定底栏，不随替补名单滚动。
- 状态块扩大为 230×54 像素级别的高对比卡片，保留等待、保存中、已保存、失败和并发冲突状态；保存中增加脉冲提示，移动端改为占满替补栏宽度。
- 已移除细节战术面板原空页脚；专项静态回归 1/1、JavaScript 语法和差异检查通过。按用户要求未进行浏览器测试。
- 本改动尚未制作生产热更新包。

## 球员卡与所有权回收性能（2026-08-14 后续改动）

- 已确认卡顿由完整联赛响应、两次连续背包整页渲染以及单体 JSON 写盘叠加造成；Axero 本地样本包含约 680 张球员卡。
- 单卡/批量单卡回收与单人/批量所有权回收接口现统一返回 compact 回执，不再重建或传输完整 `ownTeam`、球员目录及比赛等无关视图。
- compact 回执包含删除卡片、删除球员、返还所有权、受影响球员、名单占用和必要阵容状态增量；前端会同步清理卡片仓库、球员家族、所有权与首发/站位缓存。
- 确认回收后改为请求阶段禁止自动渲染，清理选择状态与弹窗后仅执行一次页面渲染。Axero 680 卡隔离样本的单卡 compact 回执约 1.2KB、服务逻辑约 6ms，不含实际磁盘和网络耗时；旧完整视图响应约 1.52MB。
- 性能专项 8/8、回收相关回归 32/32、JavaScript 语法与差异检查通过；按用户要求未进行浏览器测试，尚未制作生产热更新包。

## V9 后续合并热更新（2026-08-14 19:19）

- 已将金球奖 V3、系统回收上限 `+6`、战术自动保存状态位置和球员卡/所有权回收性能优化合并为生产热更新。
- 包：`handoff/football-ydl-s4-post-v9-ballon-recovery-tactics-hotfix-20260814-191936.tar.gz`。
- SHA256：`a449e905fa84a6d0fa01533d610879d6e36dc13e2123c03b5e5f694450251b46`。
- 仅包含 `versus/api.js`、`versus/league-service.js`、`versus/public/app.js`、`versus/public/styles.css`，前置条件为预测市场 V9 已上线。
- 重新验证：金球奖 3/3、回收规则与保存提示 13/13、回收性能 2/2；包内哈希、外层哈希、解压后 JavaScript 语法检查均通过。按用户要求未进行浏览器测试。

## 金球奖历史复核 V2 修正（2026-08-14 19:49）

- 线上只读检查确认 S11–S16 的完整归档文件仍存在且包含球员统计、杯赛和积分榜；V1 误报缺失是因为复核只读取了分片归档摘要。
- 已新增完整归档按需读取，公告标签改用荣誉室顺序 S11–S17，复核版本升级为 V2；V1 已完成的 S17 改判不会重复发币或增加标识。
- 修正包：`handoff/football-ydl-s4-ballon-reconciliation-v2-hotfix-20260814-194924.tar.gz`，SHA256 `77ecb39151de1d327cc1e22d4153234ee63e30b1d06ffb7d1e236eb0ed2a9947`，尚未确认线上部署。
- 包只覆盖 `versus/league-service.js` 和 `versus/league-shard-store.js`；金球奖 3/3、分片归档读取 1/1、包内外哈希和语法检查通过。

## 分片存储压实工具（2026-08-14 20:07）

- 线上只读诊断：根分区 40GB、已用 31GB、可用 7.2GB；联赛分片约 22GB，共 10,539 个 revision，当前 manifest 自身仍引用 10,408 个 revision，备份独占仅约 134MB，不能手工删除旧编号目录。
- 已新增 `devtool/compact-league-shards.js`，支持只读精确分析、停服原子压实、健康验证后的显式 GC，并拒绝在服务运行时执行写操作。
- 工具包：`handoff/football-ydl-s4-shard-compaction-tool-20260814-200750.tar.gz`，SHA256 `c685c07166e9a441703c0d2bbcb5e20a62e0b542b9dae341d568cae0d2d6f9f0`。
- 模拟跨 revision 的 core、比赛报告和历史归档压实/GC 回归 1/1，包内外哈希与语法检查通过；尚未在生产执行压实。

## 本地交接边界

- 包含当前核心源码、相关测试、预测模拟脚本、最新生产候选和 handoff/docs 文档。
- 明确排除 `data/` 运行数据、`Cloud_league_data/`、`outputs/`、`history/`、本地工作台 JSON、旧 staging、旧展开目录、`.git/`、`node_modules/`、账号、密钥、证书和服务器存档。
- 本轮仅运行金球奖定向测试 3/3、JavaScript 语法检查和差异检查；未运行完整测试集。

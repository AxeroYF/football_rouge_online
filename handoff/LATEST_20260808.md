# YellowDogs League S4 Local Handoff

更新时间：2026-08-08（Asia/Shanghai）

这是供下一次 Codex 本地开发对话恢复上下文的交接说明，不是服务器部署包。

## 当前范围

- 唯一开发工作树：`D:\Project\game_test\.worktrees\s4`。
- 工作树已有大量用户未提交改动。禁止执行 `git reset --hard`、`git clean`、`git checkout --`，也不要批量覆盖文件。
- 生产使用稳定 V2；不要把 `versus/public/v25-broadcast.js` 或其他 V25 实验文件带入生产或本 handoff。
- `versus/automatic-substitution.js` 是当前 `league-service.js` 的运行时依赖，必须保留。
- 不要运行完整联赛长回归。用户会自行做完整和视觉测试。

## 已完成：赛事改革

- 世界杯模式、入口、后台和国家队战术板已移除；`test/world-cup.test.js` 已删除。
- 黄狗冠军杯改为 10 队单循环联赛阶段：9 轮、每轮 5 场、共 45 场，胜 3 分、平 1 分、负 0 分。
- 联赛阶段前八晋级；八强固定为 1-8、4-5、3-6、2-7。八强和半决赛为两回合，决赛单回合。
- 联赛阶段结束时，前八的真人玩家获得 2 个 `legend-random`；第 1-4 名另得 10000 金币，第 5-8 名另得 6000 金币。AI 不发放，奖励幂等；冠军仍手动发放。
- 修复：赛程页面此前把全体 9 轮待进行杯赛都使用 `cup.nextRoundAt`，导致同一球队显示同时踢 9 场。现在赛程按待进行事件顺序以 20 分钟间隔显示；实际开赛仍由原有逐轮排程控制。

## 已完成：YOOGLE

- 联赛总览右侧有常驻、居中的 YOOGLE 搜索框，支持姓名、俱乐部、国籍/国家队和位置。
- 结果点击后打开居中球员详情：默认卡、默认 26 项能力、身高、惯用脚、俱乐部、国家队、评级与所有权。
- 核心能力按主位置高亮；详情文本加大加粗。
- 可预览 +0 至 +8 强化和羁绊增益（步进 0.5%，范围 -100% 至 +100%），两者叠加更新卡面、总评和能力值；纯前端预览，不修改资产。

## 已验证

- `node --check versus/league-service.js`
- `node --check versus/public/app.js`
- `node --test --test-name-pattern="联赛总览提供YOOGLE" test/s4-assets.test.js`
- `node --test --test-name-pattern="cup schedule (follows|lists)" test/league.test.js`
- 相关 `git diff --check`

完整 `test/league.test.js` 曾长时间运行，用户要求停止；不要重新执行完整长回归。

## 主要文件

- `versus/league-service.js`: 杯赛赛制、排程展示、奖励、球员目录字段。
- `versus/public/app.js` / `versus/public/styles.css`: YOOGLE 和玩家端赛事 UI。
- `versus/api.js` / `versus/admin-api.js` / `admin/public/app.js`: 已移除世界杯相关接口与后台入口。
- `test/league.test.js` / `test/league-shards.test.js` / `test/s4-assets.test.js`: 定向回归覆盖。

## 本包排除

- `data/`、`Cloud_league_data/`、`outputs/`、`node_modules/`、`.git/`。
- 账号、密钥、服务 PID、运行存档、旧 handoff 包。
- 世界杯、国家队战术板和 V25 实验文件。

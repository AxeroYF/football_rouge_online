# 登录界面重做 · 2026-09-06

已在本地完成；未打包、未部署、未修改服务器及存档。仍遵守用户明确要求后才打包的约定。

## 最终界面
- 登录/注册期间隐藏顶部菜单栏，登录成功进入游戏后恢复。初次 HTML 就带登录态 class，避免菜单栏闪现。
- 品牌区域仅显示现有游戏图标 assets/yellowdog-logo-transparent.png 与“黄狗风云”。删除 YF 字母标记、英文名称、共享世界标语和 33 人说明。
- 登录/注册保留必要的昵称、密码、按钮与出错提示；无错误时隐藏空提示区。
- 单列居中面板，最大宽 440px，窄屏自适应；高度不足时可滚动到完整表单。
- 根据用户追加指示，最终背景是 **assets/screen-r3.png**，替换此前的 screen-r2.jpg，不再使用 screen.jpg。未删除或修改用户的原图。
- 根据用户追加指示提高透明度：面板 rgba(12,23,31,.42)，输入框 rgba(7,17,26,.28)，面板模糊从 14px 降为 8px；字体与操作按钮保持清晰。
- 球队命名/初始选卡不套用登录背景；选卡保留原有蓝色流星雨。
- 恢复顶栏后进入地图会调用 map.invalidateSize({pan:false})，适配登录与游戏页面的高度变化。

## 文件
- styles/login.css：所有登录专用视觉，排在主题样式后；输入框使用表单 ID 避免通用主题里 :is(#campaign-tactics, ...) 意外抬高选择器优先级。
- campaign-entry.js：setAuthScreen 切换登录页状态，精简品牌和表单。
- index.html：初始登录态、追加专用 CSS，版本标记 login-v4（CSS）。
- app.js：地图开始加载时重新测量尺寸。

## 地图加载页补充
- 地图加载背景也使用 screen-r3.png；与登录页共用 --campaign-entry-backdrop，保持图片、位置及遮罩一致。
- 正常加载只显示“征程地图加载中”，保留非文字加载转圈；移除说明小字及三维构建百分比回调。
- 失败时显示简短错误和原兼容地图入口，详细原因保留在控制台，避免失败后仍显示加载中。
- 本次 app.js 语法与地图相关 34 项回归通过；1920px/390px 离线检查背景、单行文案及加载完成隐藏状态通过。未打包、未部署。
- 最新样式/入口版本：entry-loading-v1。

## 验证
- campaign-entry.js / app.js 语法通过。
- 相关现有回归 53 项通过（选卡、翻牌、地图、Three、地图加载）。
- 在隔离 VM 中执行实际 campaign-entry.js：登录/注册品牌、登录到球队/选卡/游戏的状态切换、旧会话自动进入与过期会话返回登录均通过。
- 504 项登录 CSS 层叠检查通过：登录/注册、两主题、七档宽度（含 390px）。
- 1176 项选卡 CSS 检查仍通过。
- 检查产物在 outputs/login-review/；这些是离线逻辑及 CSS 层叠检查，不是浏览器截图/视觉验收。
- 没有操作浏览器、开发服务或真实账号，最终视觉可由用户在本地刷新查看。

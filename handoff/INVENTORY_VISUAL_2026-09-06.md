# 背包视觉落地 · 2026-09-06

## 本轮结果

用户批准背包视觉 Demo，并明确“不要添加背包的图标”。已接入正式前端，标题只显示“背包”。

- 左侧改为紧凑方形槽位，使用现有四种卡包图片；品质底色／底边、金色选中框、右下角数量。
- 优先排列并默认选中有库存的卡包；零库存淡化，仍可查看详情，无法开启。
- 空槽位仅作视觉装饰，不代表容量，也没有加入扩容、容量限制或 Demo 调试控件。
- 右侧显示品质、卡包图片、名称、三选一规则、S/A/B/C 基础评级概率、真实库存和开启按钮；去掉营销说明小字。
- 概率优先读取状态中的 gradeWeights，缺失时读取现有共享卡包配置；没有修改服务端概率、抽卡或消耗规则。
- 桌面两栏独立滚动；窄窗口上下排列；较矮桌面窗口仍固定保留开启按钮。图片固定在槽位内部，避免原始图片尺寸撑开布局。
- 单击保留槽位 DOM，双击开启、请求期间锁定、三选一、获得球员、返回背包及网络失败重试沿用现有流程。无关地图更新不重绘背包。

## 修改位置

- client/inventory/inventory-controller.js：槽位与详情模板、真实库存排序、选中更新。
- styles/inventory.css：仅背包窗口作用域下的新视觉及响应式规则；原开卡舞台保留。
- app.js、index.html：更新相关资源版本标记为 20260906-inventory-slots-v1。
- test/inventory-controller.test.js：补充库存排序、服务端概率优先、空库存与请求期间状态回归。

## 验证

- 完整 npm run check 通过：70 + 482 + 69，共 621 次测试执行，0 失败。日志：outputs/inventory-slots-check.log。
- 最后图片约束修正后，背包 10 项再次通过；git diff --check 通过。
- 使用独立无头 Edge、临时本地只读静态服务与模拟 API，加载实际前端模块和完整样式。club／legacy 两主题各六档窗口，共 12 组布局通过；检查图片载入、槽位比例、图片不越界、页面无横向溢出、桌面开启按钮可见。
- 11 项交互检查通过，涵盖空库存、单击节点保留、重复请求保护、实际开卡舞台、选择与返回、失败后可重试、Escape 与重新打开。浏览器运行异常为 0。
- 报告／截图：outputs/inventory-slots-review/results.json、club-1600-1000.png、club-390-850.png 等。模拟库存仅用于检查，不写入游戏数据。未在真实账号上消费卡包。

## 使用与范围

本轮仅前端，运行本地项目后 Ctrl+F5 刷新即可，不需要因本轮重启服务。未打包、未部署、未操作真实存档。继续遵守仅在用户明确要求时打包的约定。

本轮修改前源码副本：outputs/inventory-redesign-backup-20260906-221232/。工作树存在大量先前未提交修改，不要用整树回退替代定点恢复。

## 追加调整 · inventory-slots v2

按用户追加要求，左侧在各档窗口下固定 4 列，窗口最大宽度从 1180px 收至 800px，避免四列卡包被拉得过大。装饰空槽位从 16 个收为 12 个，手机继续收起额外空行。

窗口外只保留 12% 的淡遮罩，取消 backdrop 模糊和地图 brightness 滤镜，窗口自身阴影收至 0 8px 24px / 20%。用背包 ID 作用域覆盖统一主题的深色遮罩，避免 club 主题仍显示旧的大面积压暗。开包舞台背景保持原样。

相关 15 项测试通过；独立无头浏览器两主题六档共 12 组布局均为 4 列，并验证最终生效的淡遮罩、无模糊与无地图压暗。原有 11 项交互再次通过。报告与截图仍位于 outputs/inventory-slots-review/；本次未重复完整检查。版本标记已更新为 20260906-inventory-slots-v2。本地 Ctrl+F5，无需因本轮重启；未打包。


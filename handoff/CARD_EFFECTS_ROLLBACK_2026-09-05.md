# 恢复原界面，仅保留翻牌与卡画光影

版本：`20260905-card-effects-v1`。用户明确要求回退上一轮全面改版，只保留三选一翻牌动画，以及带卡画卡片的阴影、高光。

## 最终状态

- 从 `YDL_backup/card-experiences-before-20260905` 恢复上一轮修改的 25 个文件；恢复前校验备份 SHA-256，恢复后逐文件校验。没有使用 Git reset，也没有新建另一套备份。
- 原来的三选一布局、流星背景、选中与获得球员效果已恢复。仅把上轮的逐张旋转展开动作合入既有 `inventory-card-reveal`，保留原钩子及减少动态效果规则。
- 球探三选一也使用同一个翻牌动作，每个任务首次显示时播放；轮询不更换候选节点、不重启动画，同一任务重新打开时已翻开。
- 原来的合卡/强化界面、圆形按钮、合成及高级强化动画、特性三选一、结果逻辑全部恢复。此前已修好的结果留白、最近 50 条历史独立滚动继续保留。
- 原 S/A/B/C/X 卡框、评级色、强化徽章、尺寸和头像坐标恢复。只为 `has-player-profile` 卡片增加无交互的阴影渐变层、人物投影和一次 0.75 秒高光扫过。文字层位于光影之上；无卡画卡片完全沿用原外观。
- 上轮新加的卡框 SVG、球衣剪影、签约舞台标题/序号/按钮、新强化台、横向移动端卡带均撤下；新版独有的五个源文件已删除。
- 前端引用更新为新缓存版本，避免浏览器混用已撤回的 renderer 和 CSS。没有改动服务、浏览器或真实存档；Ctrl+F5 刷新即可。

## 验证

- `npm run check`：53 + 285 + 69 = **407 项通过**。
- 旧 UI 主题检查：188 项级联检查和后台检查通过，44 个 legacy 样例无变化。
- 842 名球员 × 5 个 variant 共 4,210 份卡片 markup 与备份逐项比较，移除新增的单个光影 span 后完全一致；其中 1,930 份带卡画样例增加光影，2,280 份无卡画样例不变。原卡框 CSS 整段原样保留，仅追加光影规则。
- 合卡、背包交互、球队、训练、流星实现和原 UI 样式均与备份比较；除缓存 URL 外一致。球探只增加翻牌显示状态。
- 证据：`outputs/card-effects-rollback/restore-check.json`、`card-contract-check.json`、`full-check.log`、`theme-check.log`。

上一轮 `CARD_EXPERIENCES_2026-09-05.md` 及 `outputs/card-experience-review/` 只作为已撤回方案的历史记录，不代表当前游戏界面。后续工作以本文和用户实际体验为准。

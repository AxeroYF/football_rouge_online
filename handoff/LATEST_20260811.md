# 2026-08-11 最新交接状态

## 当前开发与安全边界

- 唯一开发目录：`D:\Project\game_test\.worktrees\s4`；分支 `codex/v2.1-dynamic`，基线 `3687514`。
- 工作树包含多轮未提交且相互叠加的用户成果；禁止 `reset`、`clean`、`checkout` 或覆盖无关文件。
- 生产服应用目录 `/opt/football-s4`，上传目录 `/home/admin`，systemd 服务 `football-s4`，健康端点 `http://127.0.0.1:4318/api/health`。
- 热更新只覆盖清单内代码与静态资源，不覆盖联赛/账号分片、球员卡工作台数据、卡画源文件、密钥或其他运行数据。
- 用户通常自行进行浏览器验证；除非用户明确要求，不要启动/停止本地服务器或运行完整测试集。

## 本轮最终功能

- 正式加入“荣誉室”导航页：展示玩家俱乐部联赛冠军、杯赛冠军、拥有时才显示的世界杯冠军、金球奖、队史出场前三与队史最佳射手。
- 奖杯改为高分辨率 WebP 资源；球员记录卡使用玩家实际拥有的最高强化等级。荣誉室在赛季结算时幂等更新一次，并通过按需接口与版本缓存加载。
- 背包球员卡详情复用 YOOGLE 球员详情布局，并根据玩家拥有的强化等级显示。
- 邮件标签已修正并开始分类：类别筛选、未读计数、批量已读、批量删除、紧凑回执；分类按钮字号已放大。强化成功只有结果达到 `+6/+7/+8` 才发送全服邮件，`+5` 保持私密。
- 预测系统展示具体让球数，继续隐藏赔率与内部概率。
- 战术板磁贴增加体力颜色：不低于体力红线为绿色，低于红线为红黄色；体力红线由滑动条改为校验范围 `45-100` 的数字输入。
- 传奇/强化单卡市场若交易同时转移所有权，商品能力信息显示“附带所有权”；实时所有权核对复用卖家/球员卡统计，避免重复全量计算。
- 普通 A/B 卡 `+8` 使用专属红金框；R3 修复内层装饰遮住球员图片和文字的问题。
- 荣誉室、背包详情和邮件分类均使用懒加载、紧凑响应、版本缓存或局部刷新，避免服务器每次请求重建完整玩家视图。

## 最新生产热更新（尚未确认线上部署）

- 推荐包：`handoff/football-ydl-s4-honor-mail-tactics-hotfix-20260811-161002-r3.tar.gz`。
- SHA256：`5d4bc2b3d9b90e1013f9d8f1b03384a78c2fe3c228142f080fc01f77c4667e44`。
- R3 取代 `football-ydl-s4-honor-mail-tactics-hotfix-20260811-135659-r2.tar.gz`、`...-131324.tar.gz` 和重叠的 `football-ydl-s4-pack-market-legend-trait-hotfix-20260811-003616.tar.gz`；不要在 R3 后再次部署旧包。
- 用户明确表示 R2 尚未推送；R3 是本地浏览器发现 A 级 `+8` 遮挡问题后重新生成的版本。当前对话没有收到 R3 已部署成功的确认。
- 包内运行文件清单与逐文件校验见 R3 目录的 `RUNTIME_FILES.txt`、`RUNTIME_SHA256SUMS.txt`、`MANIFEST.txt`、`DEPLOY.md`。
- 无需 `npm install`、数据迁移或分片恢复。

## 生产服务器存储结论

- 旧快照 `/opt/football-s4/data/yellowdogs-league-shards-before-balance-card-art-20260805-093200` 已核对为非活动目录后删除。
- 根分区由约 98% 降至 70%，当前约 12GB 可用；服务保持 active，`/api/health` 返回 `ok:true`。
- 活动分片目录为 `/opt/football-s4/data/yellowdogs-league-shards`。3850 个 numeric revision 中 3845 个仍被当前或历史 manifest 引用；当前 manifest 自身引用 3805 个目录，自动清理候选仅 4 个。
- 结论：18GB 主体是长期增量分片布局，不是可随意删除的垃圾。禁止手动删除 `revisions/*` 或 manifest；删除历史 manifest 只能释放极少目录且会损失回滚能力。
- 后续正确方向是编写、离线验证并执行“读取当前逻辑状态 → forceFull 写入全新紧凑目录 → 完整校验 → 原子切换”的专用压缩工具；当前尚未实现，不要在线临时拼接删除命令。
- `/swapfile` 是 Linux 交换文件，当前即使使用率为 0 也不建议直接删除。

## 2026-08-11 赛季存档分析

- 分析源：`D:\Project\game_test\.worktrees\s4\Cloud_league_data\0811\ydl-season-2026-08-11.json`，约 31MB；不打入交接 ZIP。
- 只分析真人对真人：116 场（联赛 72、杯赛 44），AI 与 AI 杯赛冠军排除。
- 结论：不是纯战术版本，而是“数值打底、体系决定上限”。平均 OVR 是最稳定单因素；同场 OVR 领先至少 0.5 的一方为 42胜30平19负、1.71 分/场，但仍有 19 负。
- 高位压迫 27 次使用、1.70 分/场；摆大巴 22 次、0.95 分/场。5-3-2 为 1.70 分/场，4-1-5 为 0.80。样本受具体球队选择影响，不能当作纯因果结论。
- 真人比赛共 46 次伤病，其中 30 次由对手犯规、16 次为比赛/天气伤病；雷暴 12 场场均 1.00 次伤病，含 8 次雷击伤病。红黄牌合计 407 黄、66 红。
- 最佳战术教练评为铁血蓝白AFA的 Axero：平均 OVR 94.53，真人杯赛 7胜3平2负；`5-3-2 + 积极进攻 + 高位压迫` 5 场 4胜1平、xG 净胜 `+1.66/场`。皇家马德里是成绩最佳教练与联赛冠军。
- 羁绊字段没有随赛季导出，无法从该存档独立拆分羁绊贡献。
- 图表产物会随新交接 ZIP 放入 `analysis/season-0811-tactical-analysis.html` 与 `.png`。

## 最新本地对话交接包

- 最新 ZIP：`handoff/football-ydl-s4-handoff-20260811-1812.zip`。
- SHA256 文件：`handoff/football-ydl-s4-handoff-20260811-1812.zip.sha256`。
- 交接 ZIP 用于恢复开发上下文，不是生产整体部署包；生产部署只使用上面的 R3 tar.gz。

## 验证边界

- R3 打包前已核对运行文件语法、运行清单和逐文件 SHA256；相关定向测试结果写在 R3 发布目录。
- 0811 分析图表解析 116 场真人比赛、9 支玩家队，生成 10 个图表和 9 行球队/伤病明细；离线 PNG 渲染无页面脚本错误。
- 本次 handoff 更新只修改交接文档并重新打包，不运行完整测试集、不改业务源码、不触碰服务器。

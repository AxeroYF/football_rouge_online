# 设施系统规则调整 · 2026-09-04

球探功能最新状态：先读 SCOUTING_2026-09-04.md。每人最多 5 座球探中心（含施工中），每次 500 金币 / 10 分钟 / 3 选 1；独立重复卡、低概率 +1～+3 与传奇叠加已接入。右上角共用任务栏显示进度并提供打开入口，三选一复用背包流星雨。地图倍率窗口已移除，20× 缩放保留。后端需要用户重启 4370，未操作服务或浏览器。

低倍率图标最新调整：设施、战棋按 2^(zoom-5.8) 缩放，最低约 20%；总览设施约 10px，战棋 15×16px，细节倍率恢复原尺寸，设施在高倍率继续放大至 1.8 倍（图标约 94px），战棋维持原细节尺寸。两者在 zoom 事件实时更新，战棋保留原 DOM 与地理脚部锚点，阴影随尺寸缩小；最高地图倍率仍为 20×。

本文件是设施规则的最新依据，优先于早期 7 槽位和“设施”收纳点说明。

- 首都固定总槽位 3，自动主场占用 1，可手动建设 2 座。其他地块固定 1 槽位。建筑类型仍为原有 7 类，费用、工期、权限与暂不升级保持现状。
- 服务端按总设施数检查容量；施工中的设施也占槽。无空槽时不展示可建类型，拒绝追加建造且不扣金币。暂不叠加槽位加成。
- 地图直接显示每座建筑的图标、等级/施工状态，不再使用“设施 N”收纳点、展开环或射线。首都多座并列，旧存档超过三座时仍全部展示，不静默删其他账号设施。
- 从最低地图倍率即可显示，随缩放调整图标尺寸。点击直接图标联动左上角已合并的建筑信息栏。连续世界刷新不重建未变化图标，移除设施后地图图标同步移除。

## test 账号清理已执行

用户在本轮主动停止 4370 服务后，执行 `node scripts/reset-test-buildings.mjs --apply`。

- 账号 test / YF-DCB506E6，拥有 8 块领地。
- 从 6 块领地移除 11 座非主场设施，仅保留 `building:bootstrap:test-stadium`（皇家马德里主体育场）。
- 账号全部字段及其他账号、领地归属、金币、卡包、球员、比赛记录保持原值。只修改 test 所属地块的建筑列表、旧球探中心字段（如有）及必要的版本计数。
- 清理前完整备份：`data/backups/test-buildings-before-2026-09-04T09-39-38-776Z.json`。存档和备份都被 Git 忽略，不推送。
- 清理脚本默认仅预演，`--apply` 才写入；写入前确认服务停机，备份原文并校验未被并发修改，临时文件落盘后替换。此脚本不会自动运行，重启不会再次删除后来新建的设施。
- 用实际 CampaignService 启动迁移在内存中验证：剩余设施为 1 座主场，首都 3 总槽位 / 1 已用 / 2 可用，没有重新生成其他设施。

## 验证与运行

`npm run check` 通过：51 项预检、140 项主测试、46 项 Three 专项。覆盖首都满槽拒绝扣款、普通地块限制、图标常显/缓存/选择联动及建筑面板异步隔离。随后移除残余展开样式并补充事件去重，再次运行建筑图标专项通过。

没有操作浏览器。服务由用户停止，尚未重新启动；请使用原来的终端 `npm run dev`，让新服务端规则生效，页面刷新后检查新图标布局。

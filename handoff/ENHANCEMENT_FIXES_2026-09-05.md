# 强化界面修正 · 2026-09-05

- 恢复共享卡牌的 S4 金属强化徽标：+1～+4 紫银、+5～+7 金色、+8 红金；使用与卡框一致的等级分档。删除球探样式对所有强化标的旧样式覆盖；训练小卡保留紧凑尺寸。
- 同名仓库按独立实例持续显示所有重复卡。主卡、副卡、结果卡和待绑定特性卡不再因占用界面位置被过滤。已选择卡显示选中标记，再点可移回；仍禁止同一实例作为主副卡，待绑定主卡仍需要绑定特性。
- 去掉前端兼容性判断中残留的 S4 私有池/X 卡限制，与当前服务端同名规则保持一致。
- 只读检查 test 存档：两组重复球员均有两个不同实例 ID；未修改账号。修复的是前端隐藏路径，尚未通过用户浏览器复现/视觉验收。
- 放大卡牌：桌面仓库最小列宽 200px、卡高至少 250px；主副卡高至少 236px。主副卡与结果并列，强化操作在其下方。浅灰绿色窗口、深绿文字，卡面保留原本深色和金属框；历史/批量窗口同色系。
- 更新前端缓存版本为 20260905-enhancement-v2。修改仅前端及测试，刷新页面即可，无需为了此次修改重启服务。
- 验证：强化服务/控制器、共享卡牌、训练控制器、球探 UI/控制器专项 49/49；app.js 语法及四份相关 CSS 解析通过。日志 .enhancement-v2-check.log。没有操作浏览器或启停服务。

## 第二次 UI 修正：浅蓝与紧凑合成区

- 根据用户截图修正合成框被纵向拉长：左栏合成区使用 max-content 高度，合成网格使用 auto 行；槽位 height:auto、align-self:start，正常状态最小高 350px，结果内容可自然增高。移除剩余高度向槽位拉伸的路径。
- 主副卡与结果保持并排，概率/能力成长/保卡/强化按钮集中在下方独立操作栏；剩余高度留给强化记录。窄屏保留分行布局。
- 主界面、工具栏、选中提示与记录/批量弹窗调整为浅蓝色；窗口 color-scheme:light。卡面金属样式保留。
- 样式缓存版本 20260905-enhancement-v3。8 项强化控制器测试通过、CSS 递归语法解析无错误；仅样式与缓存变更，没有启停服务或浏览器验收，用户刷新查看。

## 最新配色：淡紫色

用户要求将背景从浅蓝改为淡紫。窗口、槽位、工具栏、操作区及配套边框/文字已统一淡紫色系，布局与卡牌保持原样。仅配色改动，校验非颜色声明未变化；样式缓存版本为 20260905-enhancement-v4，刷新即可。

## 当前最终配色：偏紫深色

用户将整体色调改为偏紫的深色。窗口使用深紫灰背景（#1d1828），面板/槽位按深浅分层，文字改为浅灰紫，保留金色强化主按钮及球员卡自身配色。color-scheme:dark；缓存版本 20260905-enhancement-v5。仅主题调整，已核对非主题声明未变化；刷新查看。

## 最新布局：恢复用户参考图的 S4 横向强化区

- 用户提供成熟 S4 布局参考；主卡、副卡、104px 竖排操作区、结果按四列排列。
- 操作区从上到下为概率、能力成长提示、圆形强化按钮、保卡复选框与费用。移除此前卡牌下方的横向操作栏。
- 三个槽位最大宽 260px、最小高 350px，由内容决定实际高度；整行在合成区垂直居中，不随窗口剩余高度拉长。结果框使用金色边线，整体深紫主题保留。
- 680px 以下分两行，操作区仍保持竖排。缓存版本 20260905-enhancement-v6。
- CSS 递归语法解析通过；强化控制器 8/8，通过日志 .enhancement-v6-check.log。无浏览器操作、无服务启停，用户刷新验收。

## 最新修正：S4 点击语义、记录卡比例和扫描边界

- 对照 ../s4/versus/public/app.js 的 assignLeagueEnhancementCard、click/dblclick/drop 和 returnLeagueEnhancementResultToWarehouse：取消仓库已选卡再次点击自动移回的自定义行为；点击槽位移回时清结果与阶段。支持先拖入副卡后选择主卡，不同名主卡会清除不兼容副卡。结果单击保持、双击移回，待绑定特性给出与 S4 一致的提示。仍保留重复卡逐实例可见及当前同名、训练/首发标记规则。
- 记录页原 min-height:350px 拉长卡面：明确按 4:5 和 min-height:0 渲染；三张卡所在列采用同样内边距/边框占位，避免结果卡尺寸比主副卡小。
- 关闭图标错位源于通用 dialog button padding：通用按钮规则排除关闭按钮，关闭按钮 padding:0、line-height:1、grid 居中。
- X 光扫描沿用 S4 动画，但此前结果框 overflow:visible 导致超出框体。仅 phase-scanning 时 overflow:hidden + isolation:isolate，光带伪元素禁止截获点击；结果展示时保留原有溢出动效。
- 缓存版本：强化 CSS、app.js 和强化控制器入口更新到 20260905-enhancement-v7。
- 验证：强化控制器、服务、共享卡牌 33/33；CSS 递归语法解析通过。新增点击重复已选卡、点击槽位、先拖副卡/同名校验、结果单击/双击的回归。日志 .enhancement-v7-check.log。未操作浏览器、未启停服务，待用户刷新验收。

## 2026-09-05：卡槽占用、结果框与同名代表卡（最新规则）

- 用户最新要求覆盖此前“已选卡继续留在仓库”的显示行为。先按完整库存判断同名重复组，再从仓库排除主卡、副卡和结果卡；剩下的一张材料仍可选择，不再因过滤后不足两张而消失。
- 主副槽点击/拖回仓库才恢复显示；结果单击保持、双击/拖回仓库后恢复显示，也可以直接拖入主槽继续强化。选择/退回其他槽位不清掉保留的结果。批量强化要求先归还槽位中的卡，避免占槽卡被批量消耗。
- 成功/失败及等级变化文字移到结果框下方；结果框只展示卡片，最大卡宽从 200 调到 238px。深紫主题、横向 S4 布局、扫描裁剪保留。特性按钮也在框外下方，并预留高度。
- shared/config/representative-players.mjs：以 cardDefinitionId 识别同一球员（沿用强化同名定义），优先 upgradeLevel，然后累计 trainingBonuses 总和；完全相同保持原始顺序。球队列表/卡片模式、战术首发与替补、自动编队人数、保存战术和实际比赛使用同一代表卡规则。
- 卡片库存不删、不合并；隐藏副本仍保留独立 ID、编队归属与训练记录。代表卡使用自身编队归属，未擅自跨编队搬动卡片。同编队代表接替旧首发时映射三套站位、职责和队长，优先继承原首发坐标而非新卡旧替补坐标。仍须有至少 22 名不同球员才能组成完整双编队。
- 球队强化等级筛选补齐 +6/+7/+8；筛选在同名去重之后执行，不会用低等级过滤条件重新显示较弱副本。
- 版本：enhancement-v8 / representative-v1。包含后端变更，请用户重启 4370 后刷新。未操作浏览器、服务或真实账号存档。
- 验证：78 项专项通过（强化控制器/服务、同名代表、球队列表、战术、比赛、训练和预览），日志 .enhancement-v8-check.log；JS 语法和 CSS 递归解析通过。界面视觉仍由用户验收。

# 当前交接状态

更新时间：2026-08-31（Asia/Shanghai）

## 当前权威摘要（2026-08-31）

- 总路线：handoff/ROADMAP_2026-08-31.md。
- 最新详细进度：handoff/UPDATE_2026-08-31.md。
- 当前模式定位：大地图远征经营 + 异步战略推进 + S4 V2.1 自动比赛模拟，不做传统即时操作 RTS。
- 下一主线：远征队实体、路线、真实时间移动、地图战棋、抵达后挑战与休整闭环。
- 球员库：正式/基础各 842 人，统一 librarySource=YDL。
- 卡画：386 条有效注册，注册文件缺失 0；29 名 DLC4 计划无卡画球员为正常内容状态。
- 晚期传奇：坎通纳、里贝里、维托尔·拜亚等 9 人已恢复真实卡画并有专项回归测试。
- Admin：基于 S4 成熟工作台增改，支持球员与 26 项属性维护、批量制卡、卡画、暂存发布、审计和任务。
- 球员卡：共享契约/渲染器已接入球队管理、Admin 和 YOOGLE。
- YOOGLE：顶栏小搜索 + 标准窗口完整搜索已完成。
- UI：标准、加宽、小型三套窗口规范已固化。
- 远征素材：默认款 + 9 个国家款共 10 个，尚未接入运行时移动。
- 天气：1470 个地块按真实整点刷新；当前只影响比赛。地图覆盖默认关闭，开启后为无动画的颜色/雪花/雨线/雷暴纹理。
- 最新 npm run check：预检 31/31、主测试 89/89 通过。
- 浏览器测试由用户执行。

以下内容是按日期保留的历史记录；与上方摘要冲突时以上方和 UPDATE_2026-08-31.md 为准。

## 项目

黄狗风云（Rougelite 联机策略足球模式）

## 工作树

`D:\Project\game_test\.worktrees\Rougelite`

## 当前主入口

- `index.html`
- `app.js`
- `campaign-entry.js`
- `campaign-service.mjs`
- `server.mjs`
- `territory-model.js`
- `tactics-page.js`
- `formation-rules.js`
- `engine/campaign-match-engine.mjs`
- `engine/territory-ai.mjs`
- `campaign-broadcast.js`
- `styles.css`
- `s4-tactics-original.css`
- `styles/campaign-broadcast.css`

## 当前稳定能力

1. 共享地图、账号、初始建队、永久主场和领地状态已接入。
2. S4 生产球员库及运营覆盖已经本地化，球员保留完整 26 项属性。
3. S4 成熟阵容战术页已经迁入，包括完整 UI、三套站位、三阶段战术、拖动换人、替补排序、阵型限制、职责、队长和自动保存。
4. S4 V2.1 引擎及依赖已经复制到当前工作树，比赛运行时不引用 S4 文件夹。
5. 中立/豪门地块具有持久化 AI 驻军，阵型、打法、心态、难度与球员阵容均按地块固定。
6. AI 现有 11 套风格化阵型，并严格按球员主位置生成首发。
7. 九个核心国家 AI 获得平均能力加成。
8. 点击地块会加载左上角情报卡；重复点击同一地块或点击地图空白关闭。
9. 地块挑战为两回合制，总比分平局进入加时和点球，只有最终胜者占领地块。
10. S4 成熟电视台组件与 UI 已直接迁入本地，依次播放两回合比赛，不再使用临时“类 S4”直播界面。

## S4 电视台迁移状态

- `campaign-broadcast.js` 使用 S4 的电视台组件层级：工具栏、记分牌、联合球场、双队磁贴、实时战况、事件详情和比赛数据。
- `styles/campaign-broadcast.css` 已替换为本地化的 S4 电视台最终布局与视觉规则。
- 球员磁贴显示位置、主位置、总评、比赛评分、体力、队长、升级和比赛状态标识。
- 双方阵型与战术摘要按 S4 逻辑停靠在实时战况标题栏。
- 解说事件列表有固定高度和独立滚动，并在赛事刷新时保持用户当前阅读位置。
- 比赛数据适配层补齐 S4 所需的控球字段，评分读取 V2.1 的进球、助攻、射正、抢断、拦截和扑救。
- 首回合结束后可进入次回合，次回合结束后关闭直播并查看总赛果。
- 组件、样式与数据适配全部位于 Rougelite，不存在到 S4 工作树的运行时引用。

## 最新 UI 行为

- 每个比赛回合的电视台事件播放总时长按事件数量动态计算，目标约 2 分钟。
- 地图主界面的情报卡、赛果弹窗、地图开关、提示框、按钮和建队面板统一使用 S4 式圆角。
- S4 战术页内部布局不受本次主界面圆角调整影响。
- 地块情报卡默认隐藏。
- 首次点击地块时显示。
- 再次点击同一地块时取消选中并隐藏。
- 点击其他地块时切换内容。
- 点击地图空白处时隐藏。
- 卡片定位为地图内容区左上角 `12px / 12px`。
- 标题格式为“国家 - 省份”。
- 挑战按钮显示“挑战（两回合）”。

## AI 配置

核心国家代码：

`GBR, ESP, DEU, ITA, FRA, PRT, NLD, BRA, ARG`

阵型：

`4-3-3, 4-2-3-1, 4-4-2, 4-1-4-1, 4-3-1-2, 4-2-2-2, 3-4-3, 3-5-2, 3-4-2-1, 5-3-2, 5-4-1`

完整地图分布审计：

- 核心国家：597 个地块，平均总评 82.98。
- 其他国家：1333 个地块，平均总评 78.78。
- 11 套阵型在完整地图生成样本中均有出现。

## 存档结构

- 世界存档 schema：2。
- `world.aiGenerationSeed`：本赛季 AI 生成种子。
- `world.aiGarrisons`：各地块 AI 驻军。
- 驻军 schema：2。
- 战术完整状态：`account.tactics.planSnapshots.__s4V2`。
- `account.battleHistory`：保存精简战报，不保存完整转播事件。

## 最近验证

`npm run check`

结果：14/14 通过。

另外完成：

- JavaScript 语法检查通过。
- 地块情报框 CSS 花括号数量一致。
- 200 个 AI 驻军批量主位置匹配检查通过。
- 关键实现中未发现 S4 工作树运行时路径引用。

## 未执行

- 按用户要求，浏览器视觉与交互验收由用户自行完成。

## 推荐下一步

1. 用户浏览器验收地块情报卡显隐、定位和文字尺寸。
2. 浏览器验收完整 S4 电视台的球员位置、事件滚动、统计值和首回合/次回合切换。
3. 根据实际游戏节奏调整 AI 难度与总评曲线。
4. 将两回合比赛从一次性后端结算升级为可中断、可恢复的真实赛程状态机。
5. 补充玩家领地遭其他玩家或 AI 反攻的防守流程。

## 模块化重构状态

- 第一阶段基础边界已完成，详细记录见 handoff/REFACTOR_2026-08-29.md。
- 经济、挑战、选秀和跨海参数已迁移到 shared/config/。
- 足球位置、心态和打法名称已迁移到 shared/football/labels.js。
- 前端 API 会话由 client/core/campaign-api-client.js 管理。
- JSON 存档由 server/infrastructure/json-campaign-repository.mjs 管理。
- CampaignService、HTTP 路由、旧导出和存档 schema 3 保持兼容。
- 最新验证：npm run check，37/37 项测试通过。
- 第二阶段已完成显式存档 migrations、EconomyService、玩家颜色领域模块和独立 ChallengeScheduler。
- 最新验证：npm run check，41/41 项测试通过。
- campaign API handler 已独立，server.mjs 只保留应用装配、静态文件和启动职责。
- 最新验证：npm run check，43/43 项测试通过。
- ChallengeService 已独立，负责挑战创建、跨海复核、实时推进、查询、结算和恢复；CampaignService 保留兼容门面。
- 前端 campaign-store 已建立，app.js 的服务器状态写入统一通过 store，campaignState 暂保留为只读兼容镜像。
- CampaignService 已由重构前约 727 行降至 429 行。
- 最新验证：npm run check，46/46 项测试通过。
- app.js 地图基础层已拆出 campaign-map-data、campaign-map-geometry 和 territory-presentation。
- 地图资源读取、南美坐标转换以及地块颜色/提示不再内联于 app.js；app.js 已从 1116 行降至 997 行。
- 最新验证：npm run check，51/51 项测试通过。
- TerritoryController 已接管地块选择、侦察缓存、主场规则/提交、情报卡与图层刷新。
- MaritimeController 已接管测绘状态、海岸吸附、航线图层、目标选择和按钮/Esc 取消清理。
- ChallengeController 已接管挑战发起、并发锁、后台直播恢复与精简赛果。
- app.js 当前 694 行，相比重构前 1116 行减少 422 行。
- 最新验证：npm run check，54/54 项测试通过。
- 下一阶段：拆分 Leaflet 图层/城市 controller 和顶栏/键盘 application shell。
# 2026-08-28 后台比赛与地图边界更新

- 地块地图不再通过红橙色描边标记可攻击范围，避免主场附近地块出现误导性的红色边界；挑战入口仍由地块情报卡中的“挑战（两回合）”控制。
- AI 地块争夺赛默认后台播放，右上角 `campaign-live-widget` 显示两队、比分、比赛时间和回合；点击比分卡进入完整 S4 风格电视台。
- 首回合结束后进入 180000ms（3 分钟）冷却，冷却结束后才允许开启第二回合；`campaign-broadcast.js` 提供 `startCampaignBroadcastBackground` 和 `SECOND_LEG_COOLDOWN_MS`。
# 2026-08-28 S4 战术板边界规则补齐

- 战术坐标统一限制在 S4 范围：横向 8–92，纵向 6–94。
- 默认站位必须保持恰好 11 人、1 名门将，并且前场、中场、后场各至少一人；领先与落后站位必须保持恰好 11 人和 1 名门将。
- 非法站位会暂停实时保存并显示未保存状态，不能再把非法阵型写入账号数据。
- 服务端 `saveTactics` 已增加独立校验与坐标清洗，同时校验首发、三个位置预设、参考线、队长归属和完整替补名单。
# 2026-08-28 S4 三站位自动排阵规则

- 默认、领先、落后三个站位共用一份 11 人首发名单，但各自保存独立坐标和独立球员职责。
- 默认站位的“自动替换球员”可以从完整名单选择首发；发生换人时通过旧槽位到新球员的映射同步三套站位，每套站位原坐标互不覆盖。
- 领先和落后站位的“自动替换球员”只能重排默认首发 11 人，只修改当前站位，不允许调用替补，也不改变另外两套站位。
- 手动主替补交换仍属于全局首发变更，会让新球员分别继承旧球员在三套站位中的对应坐标和职责。
- 新增本地 `tactics-lineup-rules.js`，迁移 S4 唯一最优分配算法；无任何 S4 运行时路径引用。
# 2026-08-28 跨海征战机制

- 从当前地块 GeoJSON 独立生成 `assets/data/campaign-coastlines.json`，共识别 627 个拥有海岸线的地块；构建脚本为 `scripts/build-maritime-data.mjs`。
- 自己的内陆领土不显示任何挑战按钮；自己的海岸领土显示“出海征战”，不再显示普通挑战按钮。
- 玩家进入出海模式后，鼠标吸附所选地块的海岸采样点；确认后由服务器重新计算直线可达登陆地块，并在地图绘制虚线航线和登陆点。
- 航线限制为同一区域、约 900 公里内、途中不穿越第三方陆地；每 12 度方向扇区只保留最近的可见登陆地块，避免一个港口侵扰所有海岸。
- 跨海挑战请求必须携带出发领土和海岸点，服务器重新吸附并复核目标；不能绕过己方归属、海岸资格、地块保护期或航线遮挡。
- 普通地块攻击现在只认 `landNeighbors`，原有为孤岛补齐的海上邻接不再自动成为普通进攻通路。
- 海上目标会排除源地块的陆地邻居；地图空白海面点击也会参与海岸点吸附确认。


# 2026-08-28 服务器实时地块挑战

此前“推荐下一步”中的真实赛程状态机已经完成：

- 发起挑战时不再预计算两回合。
- 服务端按墙钟时间和单链切片推进，默认切片 100ms，每片最多 1 条控球链。
- 第一回合 120 秒、回合间整备 180 秒、第二回合常规时间 120 秒，必要时再实时推进最多 40 秒加时。
- 结果和领土变色只发生在第二回合真实结束后。
- 电视台每秒读取服务器快照，不再根据整场事件总数加速回放，播报密度与 S4 的链式实时推进一致。
- 活动比赛支持持久化与确定性重启续赛，随机数游标直接恢复，无重放计算尖峰。
- 最新验证：`npm run check` 32/32 通过。


# 2026-08-29 三项挑战流程修复

- 第二回合拥有独立快照键，切换时前端强制重建电视台，不能沿用第一回合统计或事件。
- 每位玩家只能同时发起一场板块挑战；服务端 409 强制约束，前端同步禁用其他挑战及出海入口。
- 结算弹窗仅保留地块、胜负与归属、双方名称和两回合总比分。
- 最新 `npm run check`：33/33 通过。


# 2026-08-29 金币基础机制

- 账号新增持久化 `gold` 与最近 200 条 `goldLedger`。
- 测试期新旧账号初始金币统一为 1,000,000。
- 前端读取 `state.wallet.gold`，在顶部栏最右侧显示千分位金币余额。
- 后续商店、强化、工资、设施统一复用服务端 `adjustGold` / `spendGold`，客户端没有直接改余额权限。
- 最新 `npm run check`：34/34 通过。


# 2026-08-29 设施后端基础

- 七类设施已参数化：主体育场、球探中心、港口、训练中心、医疗中心、体能恢复中心、俱乐部商店。
- 世界存档升级到 schema 4，地块设施统一保存在 `world.territories[territoryId].buildings`。
- 旧 `scoutingCenter` 字段会自动迁移为标准球探中心记录。
- 玩家首次选择主场后，首都自动建立 1 级主体育场；旧账号启动时自动补建。
- 首都默认拥有 7 个唯一设施槽位，普通地块默认只有 1 个槽位；服务层预留领袖技能额外槽位解析器。
- 港口只能建立在服务器海岸数据确认的沿海地块。
- 建造和升级统一从金币余额扣费并写入 `goldLedger`，费用集中在 `shared/config/buildings.mjs`。
- 主体育场支持 2–30 字符自定义名称；其他设施暂不支持改名。
- 普通设施随地块征服保留，原首都的主体育场在失守时移除。
- 已开放设施目录、地块设施查询、建造、升级和改名 HTTP API。
- 球探产出、训练成长、伤病治疗、体能恢复、港口加成和商店收入尚未启用；本轮只保留 capabilities 扩展键。
- 地图建筑聚合展示已接入；建造、升级和改名面板尚未接入。
- 最新 `npm run check`：61/61 通过。


# 2026-08-29 建筑地图聚合展示

- 建筑不再把大尺寸图标直接铺在地图上；每个有设施的地块默认只显示一个“设施”聚合节点、数量和文字列表。
- 聚合节点沿用中立豪门的展开思路：点击后，设施图标围绕节点旋转展开；再次点击、地图交互或展开豪门时自动收起。
- 收起状态不创建任何建筑图片节点；展开时才按需插入 PNG，并启用浏览器原生 `loading=lazy` 与异步解码。
- 建筑节点在地图缩放级别 4.05 以上显示，位置取地块图层中心，避免大小地块上的固定图标排布问题。
- 地块情报卡的据点栏会同时列出设施名称和等级。
- 原始 V2 PNG 资源保持不变，本轮优化的是地图初始 DOM、图片请求和解码开销。
- 最新 `npm run check`：61/61 通过。


# 2026-08-29 建筑建造测试闭环

- 测试服存档中的 `test` 与 `test2` 首都已分别补建“皇家马德里主体育场”和“加拉塔萨雷主体育场”，均为已建成状态且不扣费。
- 七类设施目录的一级建造价统一为 5,000 金币；主体育场仍由选定首都时自动免费创建。
- 手动建造会立即扣费并占用槽位，随后进入 60 秒服务器权威施工状态；刷新或重启后按 `constructionStartedAt` / `completesAt` 继续计算。
- 状态轮询或设施查询会把到期施工结算为已建成，无需额外高频施工调度器。
- 设施升级暂时关闭：目录声明 `upgradeEnabled=false`，前端不显示升级入口，旧升级接口会返回 409。
- 地块情报新增“管理/查看地块建筑”入口；建筑抽屉提供槽位摘要、现有建筑预览、施工进度、倒计时和服务器筛选后的可建菜单。
- 非己方地块只允许预览；港口只会出现在服务器确认的沿海地块菜单中；余额不足和无槽位状态会禁用或替换建造入口。
- 点击地图上展开的单座设施也会打开对应地块建筑抽屉；Esc 可关闭抽屉。
- 自动化验证：建筑面板 3/3、原有主测试集 61/61，`npm run check` 全部通过。


# 2026-08-29 建筑展开尺寸与稳定刷新修复

- 地图展开后的设施图片由 56px 提升到 80px，交互盒为 88px；移动端图片为 70px，视觉量级与中立俱乐部徽标一致。
- 建筑节点控制器保存每个地块最后一次渲染内容；五秒状态轮询内容未变化时不再调用 Leaflet `setIcon()`，展开 DOM 与动画保持稳定。
- 只有设施数量、等级、施工状态、名称或展开/收起状态真实变化时才重绘节点。
- 新增连续轮询不重建展开节点测试；建筑节点专项 4/4、建筑面板 3/3、主测试集 62/62 通过。


# 2026-08-29 地图缩放保持展开与固定图标尺寸

- 展开建筑图片固定为桌面端 96px、移动端 88px，并同步扩大环形半径，避免多设施相互遮挡。
- 删除地图 `zoomstart` 对建筑和中立豪门的自动收起逻辑。
- 已展开的中立豪门即使跨过城市默认显示阈值也继续保留；已展开的建筑即使缩放低于 4.05 也继续保留，主动收起后才按阈值隐藏。
- Leaflet 保持 `zoomAnimation` 与 `markerZoomAnimation` 开启，滚轮缩放继续平滑；标记在缩放完成后仍按固定 CSS 屏幕像素显示。
- 缓存版本更新为 `20260829-fixed-scale-orbit`。
- 地图/建筑专项 14/14、建筑面板 3/3、主测试集 64/64 通过。


# 2026-08-29 建筑信息字号与等级布局

- 建筑预览卡改为三列：图标、建筑名称、右侧等级/状态；`LV.1` 与“已建成/施工中”不再挤占名称区域。
- 地图建筑悬浮窗同步改为左右两栏，右侧单独显示等级和建造状态。
- 建筑面板标题、栏目说明、建筑名称、类型、状态、价格按钮、空状态，以及地图聚合/悬浮信息字号统一放大。
- 所有前端建筑等级统一使用 `LV.n`，移除“等级 n / 最大等级”和 `Lv.n` 格式；槽位 `已用/总数` 不受影响。
- 缓存版本更新为 `20260829-building-type-scale`。
- 建筑 UI 专项 8/8、建筑面板 3/3、主测试集 64/64 通过。


# 2026-08-29 滚轮缩放流畅度回归修复

- 回退了上一轮关闭 Leaflet `zoomAnimation` / `markerZoomAnimation` 的配置；该配置是滚轮缩放逐级跳变和卡顿的直接原因。
- 地图与标记平滑缩放动画恢复，原有 `zoomSnap=0.25` 和滚轮灵敏度保持不变。
- 建筑固定 96px、展开状态跨缩放保留、豪门跨显示阈值保留等逻辑不变。
- 前端缓存版本更新为 `20260829-smooth-map-zoom`。
- 缩放专项 14/14、建筑面板 3/3、主测试集 64/64 通过。


# 2026-08-29 设施地图专属配色

- 地图设施聚合节点改用蓝紫色 `#a18cff`，与中立豪门的金色/橙色和航海路线的青色区分。
- 数量徽标、展开连线、悬浮窗边线、LV 文字、缺省图标边框和悬浮光效统一跟随设施专属色。
- 建筑原画与建筑管理面板的功能状态色不变。
- 样式缓存版本更新为 `20260829-building-violet`。
- 建筑节点专项 5/5、建筑面板 3/3、主测试集 64/64 通过。


# 2026-08-29 地图滚轮惯性缩放

- 新增 `client/map/inertial-wheel-zoom.js`，关闭 Leaflet 原生 40ms 聚合后跳级的滚轮处理，改为 `requestAnimationFrame` 驱动的连续缩放。
- 每次滚轮输入转换为缩放速度，使用指数衰减计算后续帧位移，停止滚轮后会像地图拖动一样短暂减速收尾；反向滚动会快速削弱原方向速度。
- 缩放全程围绕鼠标指向的地图坐标保持锚定，不再以地图中心生硬跳变。
- `zoomSnap` 设为 0，滚轮停下后直接保留当前连续缩放级别；最大速度和单帧时间均设上限，避免连续滚动造成过冲或卡顿。
- 建筑与中立豪门的展开状态、固定屏幕像素尺寸以及地图拖动/双击/触控逻辑保持不变。
- 新增滚轮单位归一化、速度冲量、指数衰减、边界停止和鼠标锚点测试；最新 `npm run check`：69/69 通过。


# 2026-08-29 惯性缩放末尾闪烁与回滚修复

- 移除惯性结束时额外调用的 Leaflet `_animateZoom`；此前连续缩放完成后又播放一次刻度对齐 CSS 动画，会造成画面闪烁并向相邻刻度回滚。
- 惯性结束现在只调用 `_moveEnd` 配对关闭移动生命周期，不再重算中心、不再重置视图，也不再改变最后一帧的缩放值。
- 保留 `zoomAnimation=true` 与 `markerZoomAnimation=true`，因此双击、适配范围等其他地图动作仍可使用 Leaflet 动画。
- 回归测试明确断言结束阶段未调用二次动画，最终缩放值与最后连续帧完全一致；最新 `npm run check`：69/69 通过。


# 2026-08-29 左下角战役缩略地图

- 新增 `client/map/campaign-minimap-controller.js`，复用已加载的地块 GeoJSON 和当前世界快照绘制 250×160px 左下角缩略地图，不发起第二次地图资源请求。
- 缩略地图按中立、豪门、玩家及首都状态显示领土颜色；五秒世界状态刷新后同步更新颜色。
- 白色视口框实时跟随主地图平移、惯性缩放和窗口尺寸变化，框的大小对应玩家当前看到的地图范围。
- 只有按住白色视口框才进入拖动模式；拖动时保持主地图当前缩放级别，只改变画面中心，松开后立即结束。
- 国境线和主要城市开关从右下角移到缩略地图上沿并左对齐；Natural Earth 署名移入缩略地图内部。
- 永久主场选择面板上移，避免遮挡缩略地图；移动端缩略图缩为 190×120px。
- 主样式与应用缓存版本更新为 `20260829-minimap`。
- 新增领土配色、视口同步、拖动平移和布局接线测试；最新 `npm run check`：72/72 通过。


# 2026-08-29 后续路线图与 Admin 平台规划

- 已形成独立规划文档 `handoff/ROADMAP_ADMIN_2026-08-29.md`。
- 总体方向是不整体复制 S4 后台：保留账号、内容、比赛审计和安全框架，将联赛控制中心替换为持续世界的领地、挑战、设施和经济运营中心。
- 推荐先做 Admin/RBAC、审计日志、资源流水、统一任务、配置版本和安全批量操作，再完成三套阵容、共享体力、挑战次数、失败惩罚、PvP 金币托管和并发锁组成的征服闭环。
- 后续按港口、恢复、医疗、球探、训练、商店、体育场效果的依赖顺序接入设施，再推进球员卡经济、玩家领土战争、领袖技能、赛季和生产扩展。
- 规划文档同时记录了 S4 Admin 的保留/移除/改造清单、新后台八个功能区和推荐服务调用边界。
- 本次仅更新 handoff 文档；最近代码验证仍为 `npm run check` 72/72 通过。

## 2026-08-29 P0 Admin 最小骨架

- 新增 server/application/admin-service.mjs：管理员登录、RBAC（readonly/operator/content/superadmin）、会话、操作审计、统一任务记录、幂等键和玩家金币调整。
- 新增 server/http/admin-api-handler.mjs，接入 /api/admin/login、me、udit、	asks、	asks/complete 和 players/gold。
- server.mjs 已装配 AdminService；静态后台入口为 /admin.html。
- 管理员首个账号通过环境变量 ADMIN_BOOTSTRAP_PASSWORD 初始化，避免仓库内置默认密码。
- Admin 状态独立保存于 data/admin-state.json，不修改玩家存档 schema。
- 新增 	est/admin-service.test.js；最新 
pm run check：73/73 通过。


## 2026-08-29 Admin 登录界面

- Admin 登录页改为 S4 风格深色后台布局，包含品牌区、账号密码表单、错误提示和响应式移动端布局。
- 首次初始化默认管理员为 dmin，默认密码为 19971019；已有管理员存档不会被覆盖。
- 最新 
pm run check：73/73 通过。


## 2026-08-30 入口路径调整

- demo 游戏入口固定为 /game，/ 访问会 302 跳转到 /game。
- Admin 后台入口固定为 /admin。
- /game/ 与 /admin/ 同样可用；API 路径保持不变。


## 2026-08-29 S4 球员头像资源本地化

- 从用户指定的 ootball-s4-runtime-20260825-172250.tar.gz 提取并复制 294 张 WebP 球员头像到 ssets/player-profiles/s4-imported/。
- A 级和传奇同名头像同步补入 ssets/player-profiles/，现有球员目录的 181 个 portrait 引用全部通过文件检查。
- 定位数据复制为 ssets/data/s4-a-player-profile-positions.json 与 ssets/data/s4-legendary-profile-positions.json。
- 资源清单为 ssets/data/s4-imported-player-profile-manifest.json。运行时不引用备份或 S4 路径。


## S4 球员卡画完整迁移（2026-08-30）

- 已将 S4 球员卡画处理改为本地注册表驱动，不再依赖针对亨利等单个球员的手工适配。
- `assets/data/s4-player-profile-registry.json` 保存球员 ID 到卡画文件、卡画定位参数和来源分组的映射。
- `scripts/sync-player-profiles.mjs` 会将注册表自动应用到 `s4-player-base-catalog.json` 与 `s4-player-catalog.json`，写入 `portrait` 和 `portraitPosition`。
- `scripts/import-s4-player-profiles.mjs` 可从新的 S4 解压目录重新导入 A 级/传奇映射、卡画资源和哈希头像资源。
- `scripts/build-s4-catalog.mjs` 已接入自动同步；重建生产目录时会自动恢复所有已注册卡画。
- 当前注册表有 245 个精确球员 ID 映射，723 名球员目录已重建；无精确映射的球员不会被错误绑定卡画。
- 可执行 `npm run sync:player-profiles` 手动同步。
- 最近 `npm run check`：73/73 通过。

## 2026-08-30 Admin 与球员库完整重做

- 废弃仅含任务/金币表单的 P0 临时后台；`/admin` 已切换为以 S4 内容工作台为底座的 `admin-v2.html`。
- 新增球员库管理、球员卡工作室、数据审计、任务与操作记录四个入口。
- 正式球员支持基础资料与完整 26 项属性维护，保存后使用 S4 算法重算总评，并持久化到生产覆盖和生产目录。
- 新球员支持暂存卡、JSON 批量制卡、发布批次和选择后批量上线。
- 卡画支持上传、浏览器端转 1600px WebP、实时定位和统一注册表维护；所有写操作进入 Admin 审计。
- 实际审计：生产/基础目录各 723 人，245 条有效卡画映射，覆盖率 33.9%，539 个本地图片文件，其中 294 个为尚未绑定 ID 的 S4 候选资源。
- 数据完整性：0 个目录单边 ID、0 个重复 ID、0 个属性缺失、0 个总评算法错配、0 个缺失卡画文件、0 个孤立注册。
- 同名人工复核项：科克曲 2 人、米林科维奇 2 人，暂未擅自改名。
- 完整审计及接口说明见 `handoff/PLAYER_LIBRARY_ADMIN_2026-08-30.md`。
- 最新 `npm run check`：74/74 通过。
- `/admin` 与 `admin-v2.js` 已通过实际服务器 HTTP 200 检查；浏览器视觉/交互验收由用户执行。

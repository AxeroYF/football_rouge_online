# YellowDogs League S4 本地开发交接（2026-08-08 R3）

更新时间：2026-08-08 23:25（Asia/Shanghai）

这是下一次 Codex 本地开发对话的上下文包，不是服务器部署包。唯一开发工作树为 `D:\Project\game_test\.worktrees\s4`。工作树包含大量未提交的用户改动，禁止 `git reset --hard`、`git clean`、`git checkout --` 或用压缩包整体覆盖工作树。

## 当前生产与部署状态

- 生产稳定使用 V2；V2.5 仍是本地实验，不得把 `versus/public/v25-*` 文件带入生产。
- 用户此前确认已经部署 `football-ydl-s4-11-features-hotfix-20260808-165630` 和 `football-ydl-s4-ballon-dor-podium-hotfix-20260808-173621`。
- 最新完整热更新包：`D:\Project\game_test\handoff\football-ydl-s4-dlc3-tactics-gk-hotfix-20260808-224833-r3.tar.gz`。
- R3 SHA256：`531e7c4065b4ab5a96c5926621723fa580137c660cee4c3a00c801d9e1e5743e`。
- 最后已知服务器状态：R3 已上传到 `/home/admin`，备份已创建并已停止 `football-s4`；第一次解压因当前 shell 中 `ydl_package` 为空而失败。随后已重新确认包和校验文件存在，并向用户提供继续校验、解压、检查、启动命令。尚未收到最终 `health status=200`，下一段对话不要擅自认定 R3 已部署完成。

## 今日已完成的主要功能

### 杯赛和主页性能

- 黄狗冠军杯改为独立的 9 轮单循环赛程，杯赛对阵不再复制联赛前 9 轮。
- 杯赛未来轮次在日程页面按 20 分钟间隔展示，不会把 9 轮显示在同一时间。
- 其他玩家主页改为轻量首屏，只预加载最近 5 场比赛；完整历史按需分页加载。

### 战术板与阵容方案

- 默认/领先/落后站位，中后卫、后腰、前腰和中锋中央识别区域已拓宽；后腰、前腰和中锋宽度与中后卫一致。
- 新增位置阴影开关，展示后台识别区域和位置标注；浅色主题使用深色标注文字。
- `开局/平局战术` 统一改称 `默认战术`；移除战术板标题和多余说明小字；工具栏整理为两行。
- 支持最多 3 套阵容方案、自定义名称、各自首发、三阶段站位和战术细节；现有自动保存继续保存当前激活方案。
- 方案可设置 `所有比赛 / 联赛 / 杯赛 / 友谊赛`。离队、伤病或停赛时比赛阵容会从替补席自动补齐，不覆盖已保存方案。
- 移除“羁绊已可用”占位标记，保留羁绊功能。
- 最终修复：赛事下拉框事件此前引用渲染函数的局部变量 `assignmentValue`，会抛出 `ReferenceError`，导致选择看似改变但请求未发出。现在使用 `lineupSchemeCompetitionValue()` 从当前方案取值。
- 用户已在浏览器实测通过：原方案设为杯赛 -> 切到新方案 -> 切回原方案，仍显示杯赛。

### 比赛引擎

- 严格裁判的总出牌概率已调整到约 20%。
- 黑哨事件不再固定发生在第 1 分钟，可在全场合理时间出现。
- V2 门将被红牌罚下时，系统会换下场上评分较低（并以总评辅助排序）的中后卫，换上替补门将；普通位置红牌仍不触发换人。

### 金球奖

- 每赛季结束后，从所有玩家持有的具体球员卡中只选出一名获奖者，不按球员家族聚合。
- 资格要求联赛和杯赛合计至少 15 场；积分综合进球、助攻、平均评分、联赛/杯赛表现和球队当日冠军情况。
- 获奖卡记录累计次数并显示纵向金色地球标记；持有人获得 10000 金币。
- 全服邮件使用日期标题（如 `2026-08-08`），正文可附积分前三名；历史结果支持只查询、不重复发奖或改动存档。

### DLC3

- DLC3 最终运行库共 47 人，使用 `player_dlc3/list.txt` 的原始简化中文名和审核后的 26 项数据。
- 已加入 DLC3 S/A 卡画映射、服务器已调为 A 级的迪马利亚卡画，以及 X 球员刘祖豪卡画。
- R3 包含 35 张 A 卡、6 张 S 卡和 4 张 X 卡的 WebP/定位映射，但本地 handoff ZIP 为控制体积不重复携带 WebP；映射、数据和生成脚本会收录。

## 最近验证

- 真实浏览器回归：方案 1=`杯赛`，方案 2=`所有比赛`，切回方案 1 仍为 `杯赛`，控制台无错误。
- `node --check versus/public/app.js`。
- `node --test --test-name-pattern="lineup schemes|lineup scheme controls" test/league.test.js`：2/2 通过。
- `node --test --test-name-pattern="V2门将被红牌|lineup schemes|lineup scheme controls" test/match-engine-v2.test.js test/league.test.js`：3/3 通过。
- R3 解包验证：57 个文件；DLC3=47；卡画映射检查通过；核心文件哈希正确；未包含 `data/`、账号、凭据或环境文件。

## 注意事项

- 不要运行完整联赛长回归，除非用户明确要求；用户会自行做全量和视觉测试。
- 不要把本 handoff ZIP 当部署包。服务器部署只使用 R3 `.tar.gz` 和对应 `.sha256`。
- `versus/automatic-substitution.js` 是当前运行时依赖，不能遗漏。
- 本 handoff 排除运行存档、分片、账号、密钥、WebP 卡画、旧热更新归档、`.git/`、`node_modules/` 和 V2.5 实验文件。

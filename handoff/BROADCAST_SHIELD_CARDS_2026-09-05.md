# 电视台直播小型盾形卡 · 2026-09-05

用户要求电视台直播也接入小号球员卡。已将双方场上磁贴默认替换为静态盾形卡，沿用共享卡框、卡画与头像定位；不创建粒子 Canvas 或扫光层。

- `campaign-broadcast.js` 使用 `client/player-card/broadcast-card.js` 输出静态卡面；保持原主客队坐标转换、场上／红牌／伤退人员筛选、比赛事件和退出观赛流程。
- 球员卡按球场宽度的 10.5% 缩放，范围 32–56px，适应同场显示双方 22 人。红／蓝边框和评分标签区分主客队；保留实际位置、强化等级、评分、体力、队长、进球／助攻／红黄牌／伤退标记。无卡画时显示原评级圆章。
- 红牌和伤退只淡化卡面，外部赛事标记保持可见。
- 后端 `publicBroadcast` 原先只补充评级和强化，没有输出卡画信息；现在从实际比赛席位经共享卡片合同归一化，增加 `card` 字段中的球员身份、英文名、俱乐部、国籍和 `art`。同时支持后台 `profile` 与目录 `portraitPosition`，不额外暴露完整属性或存档状态。
- 每秒仍按原 1000ms 节奏接收快照。刷新前记录旧卡面，按“队伍序号＋球员 ID”和静态外观签名复用 DOM；体力、评分、队长及事件标记在卡面外更新。避免卡画每秒重新插入造成闪烁；卡画、能力、强化等外观数据变化时正常替换。新回合 reset 清空旧卡面。
- 旧的已完成历史快照若没有卡画元数据，仍能显示评级卡面，不请求额外目录或猜测头像。

验证：423 项完整项目检查通过（61＋293＋69）；新增双方真实卡画元数据、后台／目录定位、主客队同 ID 隔离、镜像坐标、无动态画布、比赛状态标记和逐秒卡面复用回归。另有 2,604 项离线 CSS 检查，覆盖 22 张 SABC 卡片、club／legacy 主题以及 1920／1180／560 宽度。

诊断产物：`outputs/broadcast-card-review/pitch.html`、`cascade-checks.json`、`full-check.log`。样式检查脚本 `scripts/check-broadcast-card-ui.py`；属于离线 DOM 与层叠验证，不等同于浏览器截图或视觉验收。

前端缓存版本 `20260905-broadcast-cards-v1`。本轮补充了后端直播字段，用户需要重启服务后 Ctrl+F5 刷新。未启停服务、操作浏览器或修改真实存档；未额外备份、提交或推送。

## 球场面积与可读性调整（broadcast-pitch v2）

- 去掉草坪 512px／500px 上限及桌面的固定纵横比，宽高跟随既有面板；两侧看台仅保留 28px，手机为 24px。上下保留底线卡片、位置和评分的展示空间。
- 卡片宽度改为 `clamp(42px,min(11cqw,10cqh),80px)`，同时参考球场宽高，避免较矮窗口仅按宽度过度放大。位置与评分由 7px 增至 9px；体力条 3px、队长标志 16px。
- 820px 及以下恢复球场自身 .76 纵横比，单列纵向滚动。主客队坐标、赛事状态、卡面复用、静态效果保持原逻辑。
- 现有电视台专项 4/4 通过；离线样式检查 4,340 项通过，覆盖 club／legacy 与 1920／1180／820／560／390 宽度；另确认草坪没有遗留宽度上限覆盖。未运行浏览器，实际画面由用户刷新验收。
- 只更新 `styles/campaign-broadcast.css` 和该 CSS 的入口缓存版本；本次刷新即可，无需重启服务。

## 等比放大修正（broadcast-pitch v3，当前生效）

用户要求球场比例不变，覆盖 v2 的自适应宽高方案：所有断点恢复原始 `aspect-ratio:.64`（16:25），`height:auto`。球场、看台和布局随宽度自然撑高，解除比赛区域固定高度，由原有外层窗口滚动，不新增每秒重绘后会重置的内部滚动区。两侧窄看台、放大评分和卡面保留；卡宽改为 `clamp(42px,11cqw,80px)`，固定球场比例后仅需按宽度缩放。

4,340 项离线卡片层叠检查通过，另确认 1920／1180／820／560／390 五档屏宽的球场比例、自动高度和外层滚动规则；未运行浏览器。纯 CSS，Ctrl+F5 即可。

## 窗口高度修复（broadcast-pitch v4，覆盖 v3）

用户明确否定 v3 为等比放大而拉长窗口／页面滚动的方案。桌面恢复固定视区高度：screen 采用 `auto minmax(0,1fr)`，标题栏按真实高度占位；比赛区域与左右栏填满剩余空间。球场通过容器宽高单位取 `min(100cqw,64cqh)`，高度由 16:25 比例决定，始终完整容纳；看台使用随球场尺寸变化的背景层，只保留一圈条纹，不把整列空白填成看台。右侧数据取消 390px 硬下限，正文使用原有内部滚动。小卡宽度为 `clamp(42px,13cqw,80px)`，保持先前增强的可读性。820px 及以下沿用单列布局，看台以固定比例确定可用空间。

验证：电视台／战术卡共 8 项显示专项通过；电视台 4,340 项离线样式检查通过；7 档屏宽的最终布局规则及 5 种宽高组合计算通过，产物 `outputs/broadcast-card-review/pitch-fit-checks.json`。未操作浏览器，实际视觉由用户刷新验收。CSS-only，Ctrl+F5，无需重启。

## 背景填充与实际面积修复（2026-09-06，broadcast-pitch v5，当前生效）

用户明确指出 v4 缩窄背景后留下两侧空白、草坪没有实际变大。删除 stadium::before 的跟随尺寸方案，条纹背景／边框直接恢复到完整 stadium 容器。保持窗口固定、球场 16:25 与宽高双约束，通过回收非球场区域增加草坪高度：桌面工具栏由 64px 收至 48px，比分栏 118→80px，标题改为单行 36px，面板 padding 14→8px，间距收紧；看台左右 24px，上下 44／52px。卡片及站位逻辑未改。

离线计算：1600×1216 内容区中草坪由约 516×806 增至 589×920，宽高增加 14.1%、面积增加 30.3%，总布局保持在相同内容区内。该数据属于模型计算，不是浏览器实测。另检查 7 档屏宽、4 种视区组合和 121 种草坪高度的底线卡片／评分边缘容纳；4,340 项既有样式检查通过。产物 `outputs/broadcast-card-review/pitch-growth-review.json`。未运行浏览器或启停服务，CSS-only，Ctrl+F5 刷新即可。

## 右栏高度与数据可读性（2026-09-06，broadcast-stats v1）

用户要求增高实时战况、压缩比赛数据、新增黄牌并放大字体。桌面右栏改为 `minmax(0,1fr) minmax(0,min(32%,300px))`：1070px 高度下，战况由约 626→758px、数据由 432→300px。数据框使用自动标题行＋可滚动正文，替代减去固定标题高度的计算；7 项统计每行至少 28px、2px 行距，标签 14px、数值 20px 并启用等宽数字。窄屏保留单列排列。

黄牌读取现有服务端球队累计 `stats.yellowCards`，放在红牌之前，旧快照缺省 0；不从事件条数或场上球员标记重新推算。只修改前端统计展示，球场比例和固定窗口逻辑未变。10 项既有电视台／引擎测试通过；验证主客队黄牌 2／5、红牌 1／0、7 项数据及旧快照补零，检查两套主题与六档屏宽。产物 `outputs/broadcast-card-review/stats.html`。未运行浏览器或启停服务。Ctrl+F5 刷新即可，无需重启。

## 2026-09-06 比分栏可读性

> 最新电视台比分栏（2026-09-06，broadcast-scoreboard v1）：移除两侧队名下的人数／阵型；桌面队名 14→24px，比分 42→52px，保留 80px 比分栏及球场可用高度。长队名最多两行、悬停可见全名；平板队名 20px／比分 48px，手机 14px／40px。比赛阶段、时间、天气及实时战况中的战术信息保留。4 项直播测试、直播／结束页真实标记检查及两主题七档屏宽 136 项离线检查通过；仅前端，Ctrl+F5 刷新即可，无需重启。

修改 campaign-broadcast.js、styles/campaign-broadcast.css，app.js 与 index.html 缓存入口为 20260906-broadcast-scoreboard-v1。比分两侧改成单行网格，左右内容垂直居中；minmax(0,1fr) 与两行截断限制长队名宽度。验证用输出 outputs/broadcast-card-review/scoreboard.html，仅离线检查，未操作浏览器或服务。

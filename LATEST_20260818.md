# YellowDogs League S4 最新交接状态

更新时间：2026-08-18（Asia/Shanghai）

## 本轮已完成

- 大名单清退已实现，固定上限为 33 人；购买大名单扩容道具接口已禁止，玩家商店前端隐藏该商品。
- 管理员后台已增加大名单状态查看和手动清退入口：`GET /api/admin/league/roster-enforcement/preview`、`POST /api/admin/league/roster-enforcement/apply`。
- 清退必须人工确认 `APPLY_ROSTER_LIMIT_33`，服务启动不会自动执行；执行前自动备份并写入审计信息。
- 超限数量对应的随机球员会被锁定，锁定球员不能上场或合卡；所有权回收使名单回到 33 人以内后解除锁定。
- 玩家背包和单卡回收界面已显示“超限锁定”及不可上场/合卡原因。
- 管理员后台新增三种定向礼包：传奇指定 `+2`、传奇指定 `+1`、私有池指定 `+3`。礼包不进入玩家商店。
- 私有池礼包在展示候选和最终领取时均检查当前私有池所有权，名单变更后不能领取无效球员。

## 最新生产热更新

- 包：`handoff/football-ydl-s4-roster-enforcement-designated-packs-hotfix-20260818.tar.gz`
- SHA-256：`1762d36291ec6838c592be2c36cf87ab09cd6747e8431861346f5e307a75f8f4`
- 运行文件：`versus/league-service.js`、`versus/s4-assets.js`、`versus/admin-api.js`、`versus/public/app.js`、`versus/public/styles.css`、`admin/public/app.js`、`admin/public/league.css`
- 服务器已确认上传并完成 `sha256sum -c` 校验；尚未收到部署后 `systemctl is-active` 与 `/api/health` 的确认，不能视为已上线。
- 部署步骤和回滚边界见该包内 `DEPLOY.md`。生产部署应逐文件 `install`，不要整体覆盖 `data` 或分片目录。

## 工作树边界

- 权威工作树：`D:\Project\game_test\.worktrees\s4`
- 当前存在大量未提交改动；禁止 `reset`、`clean`、整体 `checkout`、删除无关文件或提交 git。
- 本交接包只用于恢复开发上下文，不是生产整体部署包；不包含运行数据、分片、账号、密钥、`node_modules`、大型模拟输出或卡画资源。

## 接手后优先核验

1. 确认服务器热更新后的服务状态和 `curl -fsS http://127.0.0.1:4318/api/health`。
2. 使用管理员后台或接口查看 roster enforcement preview，确认超限玩家统计和锁定状态。
3. 确认玩家商店不再返回/显示大名单扩容道具。
4. 确认背包锁定卡显示超限原因，并验证回收到 33 人以内后解除锁定。
5. 查看三种管理员礼包的目录、发放、传奇候选和私有池最终校验。


# YellowDogs League S4 最新交接状态（2026-08-16）

## 当前最高优先级

- 唯一开发目录：`D:\Project\game_test\.worktrees\s4`。
- 当前分支：`codex/v2.1-dynamic`；Git 基线：`40e22d2`（tag `S4-0813`）。
- 工作树包含大量未提交成果，禁止 `git reset`、`git clean`、整体 `checkout`、覆盖工作树、擅自提交或推送。
- 最近重点是默认职责修复、战术板动态阵型预览、定位球主罚人明确化，以及 V2.1 体力机制审查。

## 默认职责修复

- `resolveV2PlayerDuty()` 只接受当前位置允许的专项职责；无效或缺失职责返回默认中性职责，不再错误继承其他位置职责。
- 默认职责不再带过强的进攻压上或防守退防倾向。
- 比赛初始化、战术切换、伤病换人和联赛保存均统一走职责解析。
- 主要文件：`versus/v2/player-duties-v2.js`、`versus/v2/match-engine-v2.js`、`versus/league-service.js`。

## 战术板动态阵型预览

- 当前模型版本：`dynamic-shape-v2.1-stable.9`。
- 支持默认、进攻、防守三种落位，读取当前阵型位置、阵型线、心态、八项战术参数、攻防细节、防线高度与球员职责。
- 预览时球员磁贴变为小圆点，圆点内显示位置，圆点下显示职责；移动轨迹为带箭头虚线。
- 已优化中锋、边锋、AM、DM、中后卫职责差异，以及比赛心态、防线高度对前中后场的联动。
- 前端会调用 `POST /api/versus/league/team/tactical-shape-preview`；路由必须存在于 `versus/api.js`。
- 主要文件：`versus/v2/dynamic-shape-v2.js`、`versus/v2/tactical-shape-preview-v2.js`、`versus/league-service.js`、`versus/api.js`、`versus/public/app.js`、`versus/public/styles.css`。

## 定位球主罚人

- `v2SelectSetPieceTaker(match, teamIndex, kind)` 已成为统一入口。
- 角球、任意球和普通定位球由场上非门将中 `setPieces` 最高者主罚。
- 点球由场上非门将中 `finishing` 最高者主罚。
- 数值相同按球员 ID 稳定排序，不再加权随机选择。
- 主要文件：`versus/v2/match-engine-v2.js`、`test/player-duties-v2.test.js`。

## 生产热更新与部署事故

- 首包 `football-ydl-s4-dynamic-tactics-setpiece-hotfix-20260815-221900.tar.gz` 漏带 `versus/api.js`，线上出现 `league API not found`；禁止再次部署。
- 修正版：`handoff/football-ydl-s4-dynamic-tactics-setpiece-hotfix-20260815-223403.tar.gz`。
- 修正版 SHA256：`7a1afb319f91b64d45d87d3d059491317ecfa52239de188c8ad922544f3cf15e`。
- 修正版包含 10 个运行时文件和缺失 API 路由，包内外 SHA256、JS 语法与 JSON 解析均通过。
- 用户已上传并开始部署修正版；部署中 `cp -a` 因普通用户无法保留目录时间属性报错，后续应使用 `sudo install -D -m 0644` 按 `RUNTIME_FILES.txt` 逐文件安装。
- 当前对话尚未收到最终线上功能恢复确认。接手后先让用户确认 `/api/health`、服务状态和动态预览实际效果。

## V2.1 体力机制审查

- `stamina` 是基础属性：占压迫指标 24%，并决定每条控球链的体力损耗速度。
- `fitness` 是当前体能：空间模型使用 `0.72 + fitness / 357` 统一缩放所有能力指标。
- 相对 100 体能：80 体能约损失 5.6%，65 体能约损失 9.8%，45 体能约损失 15.4%，18 体能约损失 23%。
- 每条控球链结束后消耗：`0.085 × 战术负荷 × (1.2 - stamina / 300) × 职责负荷`；高压迫、进攻心态和高跑动职责会增加损耗。
- 比赛快照每条控球链重建，所以体能下降会持续影响下一条控球链，而不是只在开场计算一次。
- 联赛长期体力池只写回比赛内损耗的 36%；最低 35，未出场每轮恢复 18，默认赛前轮换红线 65，低于45不能正常进入正式比赛首发选择。
- 当前薄弱点：耐力属性差异偏弱、低体力惩罚较温和、最终射门/扑救/定位球部分直接读取属性而未完整复用体力系数、没有常规实时体力换人、伤病概率基本不读取体力。
- 本轮只完成分析，没有修改体力代码；接手后不得误报为已优化。

## 已完成验证

- 动态阵型、职责、预览专项：33/33。
- 比赛引擎、定位球、职责与控球链专项：63/63。
- 热更新包中的 JavaScript、JSON、包内 SHA256 和外层 SHA256 均通过。
- 按用户安排未运行浏览器或真机测试。

## 后续建议

1. 先确认修正版热更新在线上完成安装并恢复动态阵型预览。
2. 若继续优化体力，优先统一终结层的体力修正，再调整耐力差异和低体力曲线。
3. 若增加实时体力换人，应与现有伤病换人、红牌门将换人和赛前65红线区分。
4. 修改生产包时必须完整检查 API 路由、服务方法、前端调用和新增模块的依赖闭环。

## 交接边界

- 交接 ZIP 只用于恢复开发上下文，不是服务器部署包。
- 不包含运行数据、联赛分片、`data/` 工作台状态、`outputs/`、`history/`、APK、账号、密码、证书、私钥或服务器备份。
- 不要使用交接 ZIP 整体覆盖生产服务器。

# YellowDogs League SQL 存储迁移评估

评估时间：2026-08-12

## 结论

切换 SQL 完全可行，而且从长期维护、数据安全、查询效率和继续扩容的角度值得实施。但它不应作为一次“替换文件路径”的热更新；这是一次中等规模的持久化层改造，推荐采用 PostgreSQL，并分阶段影子写入、校验和切换。

不建议把当前完整 `service.state` 原样塞入一个 SQL JSON 字段。那样只能改变备份载体，无法获得行级事务、索引、按需加载、并发更新和磁盘空间控制等主要收益。

## 当前存储事实

当前项目支持两种持久化方式：

- `.json` 路径：每次保存整个单 JSON。
- 不以 `.json` 结尾的路径：`LeagueShardStore` 分片目录，以原子 `manifest.json` 切换 revision。

分片存储已经完成以下优化：

- 通过 Proxy 跟踪 `core`、比赛、卡牌、礼包、邮件、预测、直播等脏作用域。
- 球员卡和礼包库存按账号拆分。
- 比赛报告按比赛拆分。
- 流水和卡牌交易使用可追加 NDJSON segment。
- 临时 revision 完整写入后再原子切换 manifest。
- 直播比赛另有定时 checkpoint，结算保存做了合并延迟。

但仍存在以下结构性限制：

- 服务启动时仍把比赛、比赛报告、卡牌、交易、邮件和归档全部加载进内存。
- `save()`、JSON 序列化、文件比较、`fsync` 和 rename 都在 Node 主线程同步执行。
- 脏作用域虽然减少写盘范围，但卡牌等作用域仍需遍历该作用域的全部对象并重新按账号分组。
- 每次保存产生 revision；历史 manifest 为了回滚会长期引用大量 revision，生产目录已经出现明显空间膨胀。
- 市场、邮件、比赛历史、后台统计等查询主要依赖内存数组扫描和派生索引。
- 当前状态模型和大量服务方法以同步、可直接修改的 `this.state` 为中心，不能直接换成异步数据库调用。

本地当前单 JSON 为 61,982,083 字节，主要构成如下：

| 作用域 | 体积 | 占比（约） | 数量 |
|---|---:|---:|---:|
| 历史归档 `archives` | 40.26 MB | 65.0% | 4 个归档 |
| 比赛 `matches` | 18.04 MB | 29.1% | 161 场 |
| 卡牌资产 `s4Assets` | 1.02 MB | 1.6% | 资产集合 |
| 比赛预测 | 0.71 MB | 1.1% | 预测集合 |
| 球员统计 | 0.48 MB | 0.8% | 1,611 条 |
| 礼包资产 | 0.39 MB | 0.6% | 礼包集合 |

生产环境此前记录为约 3,850 个 numeric revision，其中绝大多数仍被当前或历史 manifest 引用，活动分片目录一度约 28GB。说明当前问题已经从单文件卡顿转变为 revision 生命周期、历史数据和全量内存模型的问题。

## SQL 能带来的实际效果

### 明显收益

1. **经济操作具备真正事务**

   金币扣除、卡牌转移、市场成交、礼包消耗和流水可以在同一事务中提交，并用唯一约束和行锁防止重复购买、重复成交或半完成状态。

2. **高频更新从作用域重写变为行级更新**

   一次购买只更新钱包、道具/卡牌和流水相关行，不必遍历或比较整个资产作用域。

3. **历史数据按需加载**

   首页无需把 40MB 归档和 18MB 比赛详情全部读入内存。比赛列表读摘要，打开详情时再读取报告 JSONB。

4. **索引替代数组扫描**

   市场可按状态、球员、价格和时间分页；邮件按账号、类别、未读和时间查询；邀请按接收者和状态查询；比赛按球队、赛季和轮次查询。

5. **磁盘与备份更可控**

   PostgreSQL 的 MVCC、WAL、自动清理和物理/逻辑备份不会为每次游戏操作永久保留一整套应用 revision 引用。可以使用时间点恢复，而不依赖数千个应用目录。

6. **为多进程/多实例奠定基础**

   数据不再只能由单进程内存状态独占。不过赛程推进、每日结算和直播调度仍需 leader lock 或数据库 advisory lock，不能仅靠换数据库就直接启动多个实例。

### 不会自动解决的问题

- V2 比赛模拟本身的 CPU 消耗不会因 SQL 降低。
- `view()` 构造完整玩家视图、球员目录和统计的 CPU 成本仍需按需接口与缓存解决。
- 如果每个请求仍先从数据库读出完整联赛并重建 `service.state`，性能反而会下降。
- 如果把所有内容放进单个 JSONB 行，数据库仍会重写大对象，也无法安全处理并发经济操作。
- 网络数据库引入连接、超时和运维成本，需要连接池、超时、慢查询和备份监控。

## 推荐技术路线

### 正式服：PostgreSQL

推荐使用当前仍受支持的稳定 PostgreSQL 版本，数据库可以先与游戏服务部署在同一台机器，后续再独立。Node 侧采用轻量 PostgreSQL 驱动和显式 Repository/Transaction 层即可；当前项目业务高度定制，不建议为了迁移强行引入重型 ORM。

### 本地测试：PostgreSQL 容器或独立测试库

SQLite 可以用于快速原型，但不推荐作为正式最终方案：它仍是单文件、多写者能力有限，Node 同步驱动还可能延续事件循环阻塞问题，并且与最终 PostgreSQL 的锁和事务语义不同。

## 推荐的混合数据模型

优先关系化高频、需要约束和索引的数据；复杂且以整体读取为主的报告使用 JSONB。

### 第一批必须关系化

- `league_meta`：版本、当前赛季、更新时间和迁移状态。
- `teams`：球队和玩家归属，复杂战术方案可先放 JSONB。
- `team_players` / `player_state`：名单、首发、体力、伤停和停赛。
- `wallets`、`coin_ledger`：余额与不可变流水。
- `cards`、`card_transactions`：卡牌实例、强化、特性、状态和所有者。
- `pack_inventory`、`pack_openings`：礼包库存和开包幂等记录。
- `market_listings`、`card_trade_offers`：挂牌、成交、报价与状态。
- `friendly_invitations`：发起者、接收者、状态和过期时间。
- `inbox_messages`：收件人、类别、已读/删除状态与 payload。
- `matches`：比赛摘要、双方、赛事、轮次、状态、比分和时间。

### 第一阶段保留 JSONB

- `match_reports.report_json`：完整播报、控球链和复盘数据。
- `live_matches.checkpoint_json`：正在进行的 V2 比赛 checkpoint。
- `season_archives.archive_json`：先整体迁入，后续只在确有查询需求时拆分。
- 战术板的复杂预设、羁绊快照、金球奖和荣誉室历史可先采用 JSONB。

### 必须设置的约束

- 钱包余额非负约束。
- 卡牌实例 ID、流水 ID、开包幂等键唯一。
- 一张活动卡同一时刻只有一个有效所有者。
- 一个挂牌/交易只能完成一次。
- 邀请状态使用有限枚举并为待处理邀请建立接收者索引。
- 所有经济写操作使用事务；必要时对钱包、挂牌或卡牌行 `FOR UPDATE`。

## 对现有代码的改造方式

保留 `YellowDogsLeagueService` 作为规则和业务层，在其下增加存储接口，而不是让 API 直接写 SQL。

建议接口按用例划分：

- `LeagueRepository`：赛季、球队和核心状态。
- `AssetRepository`：卡牌、礼包、钱包与流水。
- `MarketRepository`：市场挂牌和卡牌交易。
- `MatchRepository`：摘要、报告、直播 checkpoint 和归档。
- `MailboxRepository`：邮件与友谊赛邀请。
- `UnitOfWork`：一个玩家操作对应一个数据库事务。

迁移早期仍可在内存中保留当前赛季核心和实时比赛，但历史报告、归档、邮件和市场列表应改为按需查询。经济 mutation 的 API handler 必须等待数据库事务提交后再返回成功。

## 推荐迁移阶段

### 阶段 0：建立基线

- 记录 `league.save`、市场、开包、邮件、邀请、比赛详情的 p50/p95/p99。
- 记录 Node RSS、启动时间、分片目录增长速度和每日备份耗时。
- 冻结一份可重复验证的生产存档副本。

### 阶段 1：引入 Repository，不改变生产存储

- 把最危险的经济操作包在显式 UnitOfWork 接口中。
- 保持分片 JSON 为唯一权威存储。
- 为数据库实现和内存/分片实现运行同一套契约测试。

### 阶段 2：PostgreSQL schema 与离线导入器

- 从当前 manifest 指向的逻辑状态导入，不直接遍历和猜测历史 revision。
- 导入后校验球队、比赛、余额、卡牌、活动卡、交易、邀请、邮件和流水 ID。
- 生成机器可读迁移报告和逐项差异。

### 阶段 3：影子写入

- 分片 JSON 仍为权威。
- 成功的业务 mutation 记录稳定的幂等事件，并异步投影到 PostgreSQL。
- 定期对余额、卡牌所有权、市场状态、比赛数和流水尾部做哈希/数量核对。
- 不推荐在请求中无保护地“先写 JSON 再写 SQL”；应使用可重放 outbox，避免其中一边失败后永久分叉。

### 阶段 4：灰度读取

- 先把只读、易核对的比赛历史、邮件和市场分页切到 SQL。
- 经济写入仍保持原权威，观察一个完整比赛日。
- SQL 读失败时允许临时回退旧存储，但必须记录告警。

### 阶段 5：最终停服切换

- 停止服务并冻结 mutation。
- 强制保存当前逻辑状态，重放 outbox，执行最终差异检查。
- 备份 PostgreSQL 和原分片目录。
- 切换环境变量，以 PostgreSQL 为唯一权威后启动。
- 保留旧分片为只读回滚副本，不再双向写入。

### 阶段 6：回滚窗口与清理

- 至少观察一个完整赛季或规定的稳定窗口。
- 回滚必须停止服务，明确选择 SQL 或旧分片的时间点；不能把两个方向的新数据自动合并。
- 稳定后才归档旧 revision，禁止直接在线删除活动分片。

## 风险等级

| 风险 | 等级 | 说明 |
|---|---|---|
| 数据模型改造 | 高 | 当前大量逻辑直接修改嵌套 `this.state` |
| 经济一致性 | 高 | 钱包、卡牌、市场需同事务和幂等键 |
| 历史数据导入 | 中 | 大体积但低频，可先 JSONB |
| 实时比赛 checkpoint | 中 | 可先整体 JSONB，按 code 更新 |
| API 改异步 | 中高 | PostgreSQL 写入需要在响应前等待提交 |
| 运维复杂度 | 中 | 新增数据库备份、连接和监控 |
| 性能收益确定性 | 中高 | 写入和查询明显受益；模拟 CPU 不受益 |

## 最终建议

1. 现在可以启动 SQL 迁移设计和 Repository 改造，但不要直接替换生产存储。
2. 优先迁移钱包、卡牌、市场、礼包、邀请和邮件；比赛报告与归档先 JSONB。
3. 正式方案使用 PostgreSQL；SQLite 仅用于快速概念验证，不作为最终生产存储。
4. 保留现有分片系统作为第一阶段权威和最终切换前的回滚来源。
5. 把迁移目标定义为“行级事务 + 按需读取 + 可验证切换”，而不是“把 JSON 放进数据库”。

总体评价：技术可行性高，长期收益高，短期实施风险中高。适合在当前 V2.1 平衡和战术职责稳定后作为独立里程碑推进。

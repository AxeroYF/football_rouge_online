# Rougelite 累计热更新 · 20260913-r4

本版优化汰换结果响应，恢复三选一的显式选择与确认流程，修复通知栏滚动回退，保留 r3 缓存修复及此前全部更新。此次重新打包仍使用 r4 版本；已安装旧 r4 的服务器也需重新上传两个同名文件并解压更新。

完整更新清单、上传位置和命令见同目录《更新说明.txt》。本包支持 2026-09-09 正式安装版，以及已安装 20260912-r1 / 20260913-r2 / 20260913-r3 的服务器。

适用服务 `yellowdogs-rougelite`；程序 `/opt/yellowdogs-rougelite/app`，数据 `/var/lib/yellowdogs-rougelite`，Node `/opt/yellowdogs-rougelite/node/bin/node`，环境文件 `/etc/yellowdogs-rougelite.env`，端口 4380。

上传至 `/home/admin`。`--check` 只读检查，`apply` 会短暂停服、备份程序与完整存档、覆盖并重启。健康检查失败自动回滚。不要运行首次安装器或导入账号种子。

成功后终端打印实际备份路径和回滚命令。回滚会恢复配套旧程序及更新前存档，较新存档另外保留。更新被中断时保留备份和解压包，先检查服务和备份 `BACKUP.json` 的 ready 标志，避免反复覆盖。

本包不含真实账号、密码、私有环境配置和开发存档。不会替换后台球员目录、球员头像及图鉴。`QA.json` 记录本轮 160 项相关回归、9 项卡片浏览器检查、地图入口检查，以及新增的 48 项通知相关回归、6 项滚动浏览器检查、21 项战争通知检查及打包校验结果；本次未重复执行 Linux 升级回滚演练；尚未连接或部署线上服务器。

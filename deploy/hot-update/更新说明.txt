Rougelite 服务器累计热更新 20260913-r4

本轮更新（相对 r3）：
1. 汰换确认成功后优先展示结果，仓库和地图状态在后台刷新，不再阻塞出卡。
2. 汰换确认时不再重复生成候选卡展示数据；保存、素材校验和重复请求保护保留。
3. 打开球员卡管理时暂停后台地图绘制，减轻卡片界面的渲染负担。
4. 卡包与豪门奖励保留完整三选一：候选展示、选中预览、可切换、确认领取、获得球员展示。
5. 球探单轮三选一改为选择后确认领取；多轮队列仍每轮选一、最后统一领取。
6. 脚本及样式使用内容版本，避免旧缓存覆盖更新后的界面。
7. 修复右侧通知栏消息较多时向上滚动反复回退：取消自动吸附及刷新强制滚动，新增、删除消息保留阅读位置，收起后展开恢复原位置。

此前已完成 160 项相关测试、9 项卡片浏览器检查，以及完整地图入口检查；本次通知栏修复另通过 48 项相关测试、6 项滚动浏览器检查及 21 项战争通知检查。
性能测量来自隔离样本：469 张球员卡时，汰换结果响应由 1,779,411 字节降至 6,883 字节。完整列表仍会在后台同步；该数据不是正式服实测。

保留 r3 的加载修复：
浏览器/CDN 缓存旧 JavaScript 模块，导致新旧模块混用、缺少 injurySubstitutionCandidate 导出，登录后卡在地图加载页。
现为完整浏览器模块依赖图及入口脚本添加内容哈希版本；更新后刷新首页即可获取匹配模块。
已在隔离浏览器中复现旧缓存故障，并验证修复后已有球队可以正常进入地图。

同时包含上一轮更新（相对 20260912-r1）：
1. 攻下敌方总部后结束相关战争，并修复符合历史战报条件的遗留战争状态。
2. 战争中可以结盟，盟友自动加入相关战争；互相交战的阵营不能直接结盟。
3. 优化世界动态“知道了”的响应速度，精简球探窗口的冗长说明。
4. 玩家之间支持金币、石油、球员组合交易及赠送，由对方确认后完成。
5. 商店限量传奇可选择 100,000 金币或 150 石油购买，共用库存。
6. 远征队面板按该队球员读取实时体力，修复显示留守编队体力的问题。
7. 已获得的待处理研究奖励可投入当前阵型、强化、生物研究，超出部分保留；支持重复请求保护和失败回滚。
8. 伤病换人保留主位置、副位置、同位置线、全能球员的优先级，补充外场应急候选并继承站位职责；门将仍要求可用门将替补。

这是累计包，包含上一包的程序和素材更新。可用于 2026-09-09 正式安装版或已安装 20260912-r1 / 20260913-r2 / 20260913-r3 的服务器，不需要先安装旧更新包。
不导入或重置正式服账号，不替换球员目录、头像、图鉴、环境配置、Nginx 或 Node 运行时。现有账号数据保留，并由新版程序正常读取、结算和迁移。
更新需要短暂停服重启。脚本会备份受影响程序和完整数据目录，新版健康检查失败时自动回滚。

本次为合并通知栏修复后重新打包的 r4。若此前已下载或安装旧 r4，请重新上传并覆盖以下两个同名文件，再解压并执行更新。

上传以下两个文件到 /home/admin：
yellowdogs-hot-update-20260913-r4.tar.gz
yellowdogs-hot-update-20260913-r4.tar.gz.sha256

执行：
cd /home/admin && \
sha256sum -c yellowdogs-hot-update-20260913-r4.tar.gz.sha256 && \
tar -xzf yellowdogs-hot-update-20260913-r4.tar.gz && \
sudo bash yellowdogs-hot-update-20260913-r4/update.sh --check && \
sudo bash yellowdogs-hot-update-20260913-r4/update.sh apply

看到 Installed: 20260913-r4 后检查：
sudo systemctl status yellowdogs-rougelite --no-pager
curl --fail http://127.0.0.1:4380/healthz
curl --fail -I https://yellowdogsleague.online/versus/

玩家刷新网页，必要时 Ctrl+F5。
脚本会打印实际备份路径和回滚指令。备份目录在 /var/backups/yellowdogs-rougelite/hot-updates/ 下。
回滚会恢复更新前程序和存档；更新后进度不进入恢复后的存档，较新数据会单独保留。
本包仅本地构建及隔离验证，尚未部署到线上服务器。
